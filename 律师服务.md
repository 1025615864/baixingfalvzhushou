

律师服务与社区服务的差距非常大。以下是完整的改造蓝图，供新 Agent 使用。

### 2.1 差距矩阵

```
┌─────────────────────────┬────────────────┬─────────────────────────────────┐
│         能力              │   当前状态      │          需要做什么               │
├─────────────────────────┼────────────────┼─────────────────────────────────┤
│ 认证中间件               │      ❌        │ 从社区服务复制 auth.py 并适配     │
│ Service 层               │   ✅ 3个       │ 功能补全 + 权限控制              │
│ Kafka Producer           │   ✅ 基础      │ 升级为 TracingProducer          │
│ Kafka Consumer           │      ❌        │ 新建：消费用户事件               │
│ Redis 缓存               │      ❌        │ 律师列表 + 详情缓存              │
│ 限流中间件               │      ❌        │ 咨询创建限流                     │
│ 健康检查                 │      ❌        │ /health + /health/ready          │
│ OpenTelemetry            │      ❌        │ init_telemetry()                │
│ Consul 注册              │      ❌        │ 生命周期注册/注销                │
│ 统一 CORS                │      ❌        │ get_cors_config()               │
│ 错误码体系               │      ❌        │ LegalErrorCode 枚举             │
│ 结构化日志               │      ❌        │ JSON + trace_id                 │
│ Alembic 迁移             │      ❌        │ 初始迁移 + 索引优化              │
│ Dockerfile               │      ❌        │ 新建                            │
│ 监控指标                 │      ❌        │ 咨询/律师/律所核心指标           │
│ 单元测试                 │      ❌        │ Service 层测试                   │
│ 集成测试                 │      ❌        │ API 端点测试                     │
│ Schemas 层               │      ❌        │ Pydantic 请求/响应分离           │
│ gRPC 服务                │   ✅ 基础      │ 补充方法 + 健康检查              │
│ 内容审核                 │      ❌        │ 咨询内容审核                     │
└─────────────────────────┴────────────────┴─────────────────────────────────┘
```

### 2.2 目标目录结构

```
services/legal-service/
├── app/
│   ├── main.py                      # ← 重构：完整生命周期管理
│   ├── database.py
│   │
│   ├── config/
│   │   └── settings.py              # ← 增强：集成 ConfigManager
│   │
│   ├── models/                      # ← 拆分：从单文件拆为多文件
│   │   ├── __init__.py
│   │   ├── consultation.py          # Consultation + ChatMessage
│   │   ├── lawyer.py                # Lawyer + LawyerSchedule
│   │   ├── firm.py                  # LawFirm
│   │   ├── appointment.py           # LawyerConsultation (预约)
│   │   ├── review.py                # ← 新增：用户评价模型
│   │   └── document.py              # ← 新增：法律文书模型
│   │
│   ├── schemas/                     # ← 新增
│   │   ├── __init__.py
│   │   ├── consultation_schema.py
│   │   ├── lawyer_schema.py
│   │   ├── firm_schema.py
│   │   └── common_schema.py
│   │
│   ├── routers/                     # ← 扩展
│   │   ├── consultation.py          # 咨询路由（增加更多端点）
│   │   ├── lawyer.py                # 律师路由（增加更多端点）
│   │   ├── firm.py                  # 律所路由
│   │   ├── review.py                # ← 新增：评价路由
│   │   ├── appointment.py           # ← 新增：预约路由
│   │   └── admin.py                 # ← 新增：管理后台路由
│   │
│   ├── services/                    # ← 扩展
│   │   ├── consultation_service.py  # 增强：完整咨询生命周期
│   │   ├── lawyer_service.py        # 增强：认证+排班+评分
│   │   ├── firm_service.py
│   │   ├── review_service.py        # ← 新增
│   │   ├── appointment_service.py   # ← 新增
│   │   ├── matching_service.py      # ← 新增：律师智能匹配
│   │   └── metrics_service.py       # ← 新增
│   │
│   ├── clients/                     # ← 新增
│   │   ├── __init__.py
│   │   ├── user_client.py           # 用户服务客户端
│   │   └── ai_client.py             # AI服务客户端（智能匹配）
│   │
│   ├── middleware/                   # ← 新增
│   │   ├── auth.py                  # 认证中间件
│   │   └── rate_limit.py            # 限流
│   │
│   ├── events/                      # ← 增强
│   │   ├── legal_events.py          # 事件定义（扩展）
│   │   ├── producer.py              # TracingProducer 替换
│   │   └── consumer.py              # ← 新增：消费用户事件
│   │
│   ├── cache/                       # ← 新增
│   │   ├── lawyer_cache.py          # 律师列表/详情缓存
│   │   └── firm_cache.py            # 律所缓存
│   │
│   ├── errors/                      # ← 新增
│   │   ├── error_codes.py
│   │   └── exception_handlers.py
│   │
│   ├── utils/
│   │   └── logging_config.py        # ← 新增
│   │
│   └── grpc_server.py               # 增强：补充方法
│
├── alembic/                         # ← 新增
│   ├── alembic.ini
│   ├── env.py
│   └── versions/
│       ├── 001_initial_tables.py
│       └── 002_add_indexes.py
│
├── tests/                           # ← 新增
│   ├── test_consultation_service.py
│   ├── test_lawyer_service.py
│   ├── test_api_integration.py
│   └── conftest.py
│
├── Dockerfile                       # ← 新增
└── requirements.txt                 # ← 更新
```

### 2.3 七大改造模块详解

---

#### 🔴 模块 L-1：基础设施集成（最高优先级）

这是让律师服务"融入"整体微服务架构的根基。社区服务已验证了这套模式，律师服务照搬即可。

**main.py 生命周期重构**：

```python
# 目标 main.py 结构
@asynccontextmanager
async def lifespan(app: FastAPI):
    # ═══ 启动 ═══
    
    # 1. 链路追踪
    init_telemetry(
        service_name="legal-service",
        service_version="1.0.0",
        otlp_endpoint=settings.OTEL_ENDPOINT
    )
    
    # 2. Consul 注册
    if settings.CONSUL_ENABLED:
        registry = get_consul_registry()
        await registry.register_service(
            service_name="legal-service",
            service_id=f"legal-service-{settings.SERVICE_PORT}",
            host=settings.SERVICE_HOST,
            port=settings.SERVICE_PORT,
            tags=["http", "grpc", "legal"],
            health_check_url=f"http://{settings.SERVICE_HOST}:{settings.SERVICE_PORT}/health"
        )
    
    # 3. Kafka TracingProducer
    app.state.event_bus = LegalEventBus()
    
    # 4. Kafka Consumer (用户事件)
    app.state.consumer = LegalEventConsumer()
    asyncio.create_task(app.state.consumer.start())
    
    # 5. Redis
    app.state.redis = await aioredis.from_url(settings.REDIS_URL)
    
    # 6. gRPC Server
    if settings.ENABLE_GRPC_SERVER:
        asyncio.create_task(start_grpc_server())
    
    # 7. 创建数据库表（开发环境）
    if settings.ENV == "development":
        async with engine.begin() as conn:
            await conn.run_sync(Base.metadata.create_all)
    
    yield
    
    # ═══ 关闭 ═══
    if settings.CONSUL_ENABLED:
        await registry.deregister_service(...)
    await app.state.redis.close()
    await app.state.event_bus.close()
    await app.state.consumer.stop()

app = FastAPI(title="Legal Service", lifespan=lifespan)

# 统一 CORS
app.add_middleware(CORSMiddleware, **get_cors_config())

# 健康检查
app.include_router(health_router)

# 业务路由
app.include_router(consultation_router, prefix="/api/v1/legal")
app.include_router(lawyer_router, prefix="/api/v1/legal")
app.include_router(firm_router, prefix="/api/v1/legal")
app.include_router(review_router, prefix="/api/v1/legal")
app.include_router(appointment_router, prefix="/api/v1/legal")
app.include_router(admin_router, prefix="/api/v1/legal/admin")
```

| # | 任务 | 工时 |
|---|------|------|
| L-1.1 | main.py 生命周期重构 | 1天 |
| L-1.2 | OpenTelemetry 初始化 | 0.5天 |
| L-1.3 | Consul 注册/注销 | 0.5天 |
| L-1.4 | 统一 CORS (get_cors_config) | 0.5天 |
| L-1.5 | 健康检查三端点 | 0.5天 |
| L-1.6 | 结构化日志 (JSON + trace_id) | 0.5天 |
| L-1.7 | Dockerfile + .dockerignore | 0.5天 |

---

#### 🔴 模块 L-2：认证 + 权限控制

```
当前: 所有端点无认证，任何人可以操作任何数据
目标: 与社区服务一致的认证模式

权限矩阵:
┌────────────────────────┬────────┬────────┬────────┬────────┐
│         操作            │ 游客   │ 用户   │ 律师   │ 管理员 │
├────────────────────────┼────────┼────────┼────────┼────────┤
│ 查看律师列表/详情       │   ✅   │   ✅   │   ✅   │   ✅  │
│ 查看律所列表/详情       │   ✅   │   ✅   │   ✅   │   ✅  │
│ 创建咨询               │   ❌   │   ✅   │   ✅   │   ✅  │
│ 查看自己的咨询          │   ❌   │   ✅   │   ✅   │   ✅  │
│ 回复咨询 (律师端)       │   ❌   │   ❌   │   ✅   │   ✅  │
│ 提交评价               │   ❌   │   ✅   │   ❌   │   ✅  │
│ 预约律师               │   ❌   │   ✅   │   ❌   │   ✅  │
│ 管理排班 (律师自己)     │   ❌   │   ❌   │   ✅   │   ✅  │
│ 认证律师               │   ❌   │   ❌   │   ❌   │   ✅  │
│ 审核律所               │   ❌   │   ❌   │   ❌   │   ✅  │
│ 数据统计               │   ❌   │   ❌   │   ❌   │   ✅  │
└────────────────────────┴────────┴────────┴────────┴────────┘
```

**三种角色的认证依赖**：

```python
# middleware/auth.py — 复用社区服务模式
async def get_current_user(request: Request) -> AuthUser: ...
async def get_current_lawyer(request: Request) -> AuthUser:
    """要求当前用户必须是已认证律师"""
    user = await get_current_user(request)
    if user.role != "lawyer":
        raise HTTPException(403, "需要律师身份")
    return user

async def get_admin_user(request: Request) -> AuthUser:
    """要求管理员"""
    user = await get_current_user(request)
    if user.role != "admin":
        raise HTTPException(403, "需要管理员权限")
    return user
```

| # | 任务 | 工时 |
|---|------|------|
| L-2.1 | auth.py 认证中间件 (复用社区模式) | 0.5天 |
| L-2.2 | user_client.py (调用 User Service) | 0.5天 |
| L-2.3 | 所有路由接入认证 | 1天 |
| L-2.4 | 律师角色 + 管理员角色权限检查 | 0.5天 |
| L-2.5 | 限流中间件 (咨询创建限流) | 0.5天 |

---

#### 🟠 模块 L-3：业务功能补全

当前只有基础 CRUD，缺少法律服务的核心业务流程。

**3A. 咨询完整生命周期**：

```
当前: 创建咨询 → 查看详情 → 发消息    (非常简陋)

目标完整流程:

用户                                      律师                    系统
 │                                         │                       │
 ├── 创建咨询                              │                       │
 │   (选择分类、描述问题)                    │                       │
 │                                         │                       │
 │   ←── Kafka: consultation.created ──────│───── 智能匹配推荐律师  │
 │                                         │                       │
 ├── 选择律师 / 接受推荐                    │                       │
 │                                         │                       │
 │   ←── Kafka: consultation.assigned ─────│                       │
 │                                         │                       │
 ├── 发送消息 ─────────────────────────────▶│ 接收消息              │
 │◀──────────────────────────────── 回复消息│                       │
 │                                         │                       │
 │   ... (多轮对话) ...                     │                       │
 │                                         │                       │
 │                               律师结束咨询│                       │
 │                                         │                       │
 │   ←── Kafka: consultation.completed ────│                       │
 │                                         │                       │
 ├── 提交评价 (1-5星 + 文字)                │                       │
 │                                         │                       │
 │   ←── Kafka: lawyer.rating_updated ─────│───── 更新律师评分      │
 │                                         │                       │
```

**需要新增的咨询端点**：

```
POST   /consultations/                     创建咨询 (已有，增强)
GET    /consultations/                     我的咨询列表 ← 新增
GET    /consultations/{id}                 咨询详情 (已有)
PATCH  /consultations/{id}/status          更新咨询状态 ← 新增
POST   /consultations/{id}/assign          分配律师 ← 新增
POST   /consultations/{id}/messages        发送消息 (已有)
GET    /consultations/{id}/messages        消息列表 (已有)
POST   /consultations/{id}/complete        结束咨询 ← 新增
POST   /consultations/{id}/review          提交评价 ← 新增
GET    /consultations/{id}/review          查看评价 ← 新增
```

**3B. 律师功能增强**：

```
当前: 列表 + 详情 + 认证

需要增加:
├── 律师自我资料编辑
│   PATCH /lawyers/me/profile
│   { specialties, introduction, experience_years, price_range }
│
├── 律师排班管理
│   GET    /lawyers/me/schedule             查看排班
│   PUT    /lawyers/me/schedule             更新排班
│   POST   /lawyers/me/schedule/available   标记可用时段
│
├── 律师咨询管理 (律师视角)
│   GET    /lawyers/me/consultations        我的咨询列表
│   GET    /lawyers/me/consultations/pending 待接咨询
│   POST   /lawyers/me/consultations/{id}/accept  接受咨询
│   POST   /lawyers/me/consultations/{id}/reject  拒绝咨询
│
├── 律师统计
│   GET    /lawyers/me/stats               我的统计
│   { total_consultations, avg_rating, response_time, revenue }
│
└── 律师搜索增强
    GET    /lawyers/search
    ?specialty=劳动纠纷&city=北京&min_rating=4.0&available=true
    (按评分/接单量/响应速度排序)
```

**3C. 预约系统（新模块）**：

```
POST   /appointments/                      创建预约
GET    /appointments/                      我的预约列表
GET    /appointments/{id}                  预约详情
PATCH  /appointments/{id}/status           更新状态
POST   /appointments/{id}/cancel           取消预约
GET    /lawyers/{id}/available-slots        查看律师可用时段

预约状态机:
PENDING → CONFIRMED → IN_PROGRESS → COMPLETED
                   → CANCELLED
       → REJECTED
```

**3D. 智能律师匹配（法律平台特色功能）**：

```python
# services/matching_service.py
class LawyerMatchingService:
    """
    根据咨询内容智能匹配律师
    
    匹配维度:
    1. 专业领域匹配 (specialties vs consultation.category)  权重 40%
    2. 地区匹配 (同城优先)                                   权重 20%
    3. 律师评分                                              权重 20%
    4. 响应速度 (历史平均)                                    权重 10%
    5. 当前负载 (进行中的咨询数)                               权重 10%
    
    可选增强:
    - 调用 AI Service 分析咨询内容 → 精准匹配专业领域
    """
    
    async def match_lawyers(
        self, consultation_id: int, top_k: int = 5
    ) -> list[LawyerMatch]:
        consultation = await self._get_consultation(consultation_id)
        
        # 候选律师
        candidates = await self._get_available_lawyers(
            specialty=consultation.category,
            city=consultation.city
        )
        
        # 评分排序
        scored = []
        for lawyer in candidates:
            score = (
                self._specialty_score(lawyer, consultation) * 0.4 +
                self._location_score(lawyer, consultation) * 0.2 +
                self._rating_score(lawyer) * 0.2 +
                self._response_speed_score(lawyer) * 0.1 +
                self._workload_score(lawyer) * 0.1
            )
            scored.append(LawyerMatch(lawyer=lawyer, score=score))
        
        return sorted(scored, key=lambda x: x.score, reverse=True)[:top_k]
```

| # | 任务 | 工时 |
|---|------|------|
| L-3.1 | 咨询生命周期完善 (状态机 + 新端点) | 2天 |
| L-3.2 | 评价系统 (Review 模型 + 路由 + Service) | 1.5天 |
| L-3.3 | 律师自我管理端点 | 1天 |
| L-3.4 | 律师排班系统 | 1.5天 |
| L-3.5 | 律师搜索增强 (多维筛选 + 排序) | 1天 |
| L-3.6 | 预约系统 (模型 + 路由 + Service) | 2天 |
| L-3.7 | 智能律师匹配 (MatchingService) | 1.5天 |
| L-3.8 | 管理后台路由 (律师审核/律所管理/统计) | 1.5天 |
| L-3.9 | Schemas 层全量创建 | 1天 |

---

#### 🟠 模块 L-4：数据层加固

```
需要做:
├── models.py 拆分为 models/ 目录 (5+ 文件)
├── Alembic 初始化 + 初始迁移
├── 新增表:
│   ├── reviews         用户评价表
│   ├── lawyer_schedules 律师排班表
│   ├── appointments     预约表
│   └── audit_logs       操作审计 (律师认证等)
│
├── 索引优化:
│   ├── idx_lawyers_specialty_status    (专业+状态复合索引)
│   ├── idx_lawyers_city_rating         (城市+评分索引)
│   ├── idx_consultations_user_status   (用户+状态索引)
│   ├── idx_consultations_lawyer_status (律师+状态索引)
│   └── idx_appointments_lawyer_time    (律师+时间索引)
│
└── datetime.utcnow 修复 (如有)
```

| # | 任务 | 工时 |
|---|------|------|
| L-4.1 | models.py 拆分 | 0.5天 |
| L-4.2 | 新增表 (reviews, schedules, appointments) | 1天 |
| L-4.3 | Alembic 初始化 + 迁移脚本 | 1天 |
| L-4.4 | 索引优化 | 0.5天 |

---

#### 🟡 模块 L-5：Kafka 事件 + 生态联通

```
┌────────────────────────────────────────────────────────────────────┐
│               Legal Service 事件生态                                │
│                                                                    │
│  ▶▶▶ 发布事件 (Producer)                                          │
│                                                                    │
│  legal.consultation.created     → Notification (通知匹配律师)      │
│  legal.consultation.assigned    → Notification (通知用户+律师)      │
│  legal.consultation.completed   → Points (+10积分)                 │
│                                 → Notification (评价提醒)          │
│  legal.consultation.cancelled   → Notification (通知对方)          │
│  legal.lawyer.verified          → Community (更新律师标识)          │
│                                 → Notification (恭喜通知)          │
│  legal.lawyer.rating_updated    → Search (更新搜索排序)            │
│  legal.appointment.created      → Notification (预约确认)          │
│  legal.appointment.reminder     → Notification (预约提醒)          │
│  legal.review.submitted         → Points (+3积分)                  │
│                                                                    │
│  ◀◀◀ 消费事件 (Consumer)                                          │
│                                                                    │
│  user.profile.updated           → 更新咨询记录中的用户信息          │
│  user.lawyer.verified           → 更新律师认证状态                  │
│  user.membership.upgraded       → 调整用户咨询配额                  │
│  payment.completed              → 确认咨询/预约付款                 │
└────────────────────────────────────────────────────────────────────┘
```

| # | 任务 | 工时 |
|---|------|------|
| L-5.1 | TracingProducer 替换 | 0.5天 |
| L-5.2 | 补全所有事件发布点 | 1天 |
| L-5.3 | Kafka Consumer 框架 | 0.5天 |
| L-5.4 | 用户事件消费处理 | 1天 |
| L-5.5 | 事件幂等性保障 | 0.5天 |

---

#### 🟡 模块 L-6：Redis 缓存 + 性能

```
Redis Key 设计:
┌──────────────────────────────┬────────┬─────────────────────────┐
│  Key                         │  TTL   │  失效策略                │
├──────────────────────────────┼────────┼─────────────────────────┤
│ lawyer:detail:{id}           │ 15min  │ 资料更新时失效           │
│ lawyer:list:{specialty}:{page}│ 5min  │ 新律师注册时失效         │
│ lawyer:search:{hash}         │ 5min   │ 搜索结果缓存             │
│ firm:detail:{id}             │ 30min  │ 信息更新时失效           │
│ firm:list:{province}:{page}  │ 10min  │ 新律所注册时失效         │
│ lawyer:schedule:{id}:{date}  │ 5min   │ 排班更新时失效           │
│ consultation:count:{user_id} │ 1hour  │ 新咨询创建时 INCR       │
│ rate:consultation:{user_id}  │ 1hour  │ 自动过期                │
└──────────────────────────────┴────────┴─────────────────────────┘
```

| # | 任务 | 工时 |
|---|------|------|
| L-6.1 | Redis 连接 + 律师缓存 | 1天 |
| L-6.2 | 律所缓存 | 0.5天 |
| L-6.3 | 缓存失效策略 | 0.5天 |

---

#### 🟡 模块 L-7：错误码 + 监控 + 测试

**错误码体系**：

```python
class LegalErrorCode(Enum):
    # 咨询错误 L1xxxx
    CONSULTATION_NOT_FOUND        = ("L14041", "咨询不存在")
    CONSULTATION_ALREADY_CLOSED   = ("L14091", "咨询已关闭")
    CONSULTATION_LIMIT_EXCEEDED   = ("L14291", "咨询次数超限")
    
    # 律师错误 L2xxxx
    LAWYER_NOT_FOUND              = ("L24041", "律师不存在")
    LAWYER_NOT_AVAILABLE          = ("L24091", "律师当前不可用")
    LAWYER_ALREADY_VERIFIED       = ("L24092", "律师已认证")
    
    # 预约错误 L3xxxx
    APPOINTMENT_NOT_FOUND         = ("L34041", "预约不存在")
    APPOINTMENT_TIME_CONFLICT     = ("L34091", "预约时间冲突")
    APPOINTMENT_SLOT_UNAVAILABLE  = ("L34092", "该时段不可预约")
    
    # 评价错误 L4xxxx
    REVIEW_ALREADY_SUBMITTED      = ("L44091", "已提交过评价")
    REVIEW_NOT_ALLOWED            = ("L44031", "无权评价此咨询")
```

**监控指标**：

```
legal_consultations_total          咨询总数 (按状态分)
legal_consultations_active         进行中咨询数
legal_consultation_duration_avg    平均咨询时长
legal_lawyers_active               在线律师数
legal_lawyers_verified             已认证律师数
legal_matching_latency             匹配耗时
legal_appointments_total           预约总数 (按状态分)
legal_reviews_avg_rating           平均评分
legal_api_requests_total           API 请求总数
legal_api_latency_histogram        API 延迟分布
```

| # | 任务 | 工时 |
|---|------|------|
| L-7.1 | 错误码体系 + 异常处理器 | 1天 |
| L-7.2 | Prometheus 监控指标 | 1天 |
| L-7.3 | Service 层单元测试 | 2天 |
| L-7.4 | API 集成测试 | 1.5天 |
| L-7.5 | 契约测试 (与 User Service) | 1天 |

---

### 2.4 律师服务实施路线图

```
Phase 1 (第1-2周) ━━ 基础设施 + 安全 ━━━━━━━━━━━━━ 🔴 地基
│
├── Week 1
│   ├── L-1.1  main.py 生命周期重构                    1天
│   ├── L-1.2  OpenTelemetry                          0.5天
│   ├── L-1.3  Consul 注册                            0.5天
│   ├── L-1.4  统一 CORS                              0.5天
│   ├── L-1.5  健康检查                               0.5天
│   ├── L-1.6  结构化日志                             0.5天
│   └── L-1.7  Dockerfile                             0.5天
│                                              小计: 4天
│
├── Week 2
│   ├── L-2.1  认证中间件                              0.5天
│   ├── L-2.2  UserClient                             0.5天
│   ├── L-2.3  所有路由接入认证                         1天
│   ├── L-2.4  角色权限控制                            0.5天
│   ├── L-2.5  限流中间件                              0.5天
│   ├── L-4.1  models 拆分                            0.5天
│   └── L-3.9  Schemas 层创建                         1天
│                                              小计: 4.5天

Phase 2 (第3-4周) ━━ 核心业务增强 ━━━━━━━━━━━━━━━━ 🟠 肉
│
├── Week 3
│   ├── L-3.1  咨询生命周期完善                        2天
│   ├── L-3.2  评价系统                               1.5天
│   ├── L-3.3  律师自我管理                            1天
│                                              小计: 4.5天
│
├── Week 4
│   ├── L-3.4  律师排班系统                            1.5天
│   ├── L-3.6  预约系统                               2天
│   ├── L-3.5  律师搜索增强                            1天
│                                              小计: 4.5天

Phase 3 (第5-6周) ━━ 数据 + 事件 + 缓存 ━━━━━━━━━━ 🟡 血管
│
├── Week 5
│   ├── L-4.2  新增表                                 1天
│   ├── L-4.3  Alembic 迁移                           1天
│   ├── L-4.4  索引优化                               0.5天
│   ├── L-5.1  TracingProducer                        0.5天
│   ├── L-5.2  事件发布补全                            1天
│                                              小计: 4天
│
├── Week 6
│   ├── L-5.3  Consumer 框架                          0.5天
│   ├── L-5.4  用户事件消费                            1天
│   ├── L-5.5  幂等性                                 0.5天
│   ├── L-6.1  Redis 律师缓存                         1天
│   ├── L-6.2  律所缓存                               0.5天
│   └── L-6.3  缓存失效                               0.5天
│                                              小计: 4天

Phase 4 (第7-8周) ━━ 智能 + 质量 + 管理 ━━━━━━━━━━ ⚪ 皮肤
│
├── Week 7
│   ├── L-3.7  智能律师匹配                            1.5天
│   ├── L-3.8  管理后台路由                            1.5天
│   ├── L-7.1  错误码体系                              1天
│                                              小计: 4天
│
├── Week 8
│   ├── L-7.2  监控指标                               1天
│   ├── L-7.3  单元测试                               2天
│   ├── L-7.4  集成测试                               1.5天
│                                              小计: 4.5天
```

---

### 2.5 律师服务完整任务总表

| # | 任务 | 优先级 | Phase | 工时 |
|---|------|--------|-------|------|
| L-1.1 | main.py 生命周期重构 | 🔴 | 1 | 1天 |
| L-1.2 | OpenTelemetry | 🔴 | 1 | 0.5天 |
| L-1.3 | Consul 注册 | 🔴 | 1 | 0.5天 |
| L-1.4 | 统一 CORS | 🔴 | 1 | 0.5天 |
| L-1.5 | 健康检查 | 🔴 | 1 | 0.5天 |
| L-1.6 | 结构化日志 | 🔴 | 1 | 0.5天 |
| L-1.7 | Dockerfile | 🔴 | 1 | 0.5天 |
| L-2.1 | 认证中间件 | 🔴 | 1 | 0.5天 |
| L-2.2 | UserClient | 🔴 | 1 | 0.5天 |
| L-2.3 | 路由接入认证 | 🔴 | 1 | 1天 |
| L-2.4 | 角色权限 | 🔴 | 1 | 0.5天 |
| L-2.5 | 限流 | 🟠 | 1 | 0.5天 |
| L-3.1 | 咨询生命周期 | 🟠 | 2 | 2天 |
| L-3.2 | 评价系统 | 🟠 | 2 | 1.5天 |
| L-3.3 | 律师自我管理 | 🟠 | 2 | 1天 |
| L-3.4 | 排班系统 | 🟠 | 2 | 1.5天 |
| L-3.5 | 搜索增强 | 🟠 | 2 | 1天 |
| L-3.6 | 预约系统 | 🟠 | 2 | 2天 |
| L-3.7 | 智能匹配 | 🟡 | 4 | 1.5天 |
| L-3.8 | 管理后台 | 🟡 | 4 | 1.5天 |
| L-3.9 | Schemas 层 | 🔴 | 1 | 1天 |
| L-4.1 | models 拆分 | 🟠 | 1 | 0.5天 |
| L-4.2 | 新增表 | 🟠 | 3 | 1天 |
| L-4.3 | Alembic | 🟠 | 3 | 1天 |
| L-4.4 | 索引优化 | 🟡 | 3 | 0.5天 |
| L-5.1 | TracingProducer | 🟡 | 3 | 0.5天 |
| L-5.2 | 事件发布补全 | 🟡 | 3 | 1天 |
| L-5.3 | Consumer 框架 | 🟡 | 3 | 0.5天 |
| L-5.4 | 用户事件消费 | 🟡 | 3 | 1天 |
| L-5.5 | 幂等性 | 🟡 | 3 | 0.5天 |
| L-6.1 | Redis 律师缓存 | 🟡 | 3 | 1天 |
| L-6.2 | 律所缓存 | 🟡 | 3 | 0.5天 |
| L-6.3 | 缓存失效 | 🟡 | 3 | 0.5天 |
| L-7.1 | 错误码体系 | 🟡 | 4 | 1天 |
| L-7.2 | 监控指标 | 🟡 | 4 | 1天 |
| L-7.3 | 单元测试 | 🟡 | 4 | 2天 |
| L-7.4 | 集成测试 | ⚪ | 4 | 1.5天 |
| L-7.5 | 契约测试 | ⚪ | 4 | 1天 |

**律师服务总计：约 34 个工作日（7-8 周）**

---

## 三、两个服务的并行策略

```
         Week 1    Week 2    Week 3    Week 4    Week 5    Week 6    Week 7    Week 8
         ───────   ───────   ───────   ───────   ───────   ───────   ───────   ───────
社区:    ██ 收尾    █ 测试    ░ 体验     ░ 体验                         (完成)
律师:    ████████  ████████  ████████  ████████  ████████  ████████  ████████  ████████
         基础设施   认证权限   咨询增强   预约排班   数据+事件  缓存联通   智能+管理  测试
```

**建议**：

- 律师服务作为 **新 Agent 的主力战场**，8 周持续推进
- 律师服务 Phase 1（基础设施+认证）可大量复用社区服务代码，加速显著

