# 律师服务验收评审报告

---

## 🏆 总评：有条件通过验收

```
┌─────────────────────────────────────────────────────────┐
│                                                         │
│   验收结论：  ✅ 有条件通过                               │
│   成熟度：    92%                                        │
│   评级：      生产可用（Production Ready with caveats）   │
│                                                         │
│   必须修复项：  3 项  （不修复不能上线）                    │
│   建议修复项：  5 项  （上线后 2 周内完成）                 │
│   可延后项：    4 项  （按迭代节奏推进）                    │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 🔴 必须修复（不修复不能上线）— 预计 3 天

### 必修 1：领域隔离只完成了 1/7

```
当前状态：
  app/domains/firm/        ✅ 已隔离
  app/models/              ← 其余 12 个模型仍在扁平结构
  app/services/            ← 其余 11 个服务仍在扁平结构
  app/routers/             ← 其余 11 个路由仍在扁平结构

问题：
  律所领域已隔离，但咨询/预约/评价/文书等仍是扁平结构。
  这意味着：
  - 状态机放在 shared/ 里，但对应的 service 在 services/ 里
  - 新成员看到两种组织方式并存，会困惑"该放哪里"
  - 随时间推移会退化回扁平结构
```

```
✅ 要求：完成所有 7 个领域的隔离

app/
├── domains/
│   ├── consultation/       # 移入 consultation models/service/router/schemas
│   ├── lawyer/             # 移入 lawyer + schedule
│   ├── firm/               # ✅ 已完成
│   ├── appointment/        # 移入 appointment
│   ├── review/             # 移入 review
│   ├── document/           # 移入 document + template
│   └── admin/              # 移入 admin + cache_admin + metrics
│
├── shared/                 # 保持不变
│   ├── permissions.py
│   ├── state_machines.py
│   ├── database.py
│   ├── middleware/
│   ├── events/
│   ├── cache/
│   ├── clients/
│   └── errors/
│
└── main.py

工作量：1.5天（纯文件移动+import路径修改，零逻辑变更）
风险：低（URL不变，只是代码组织变化）
```

---

### 必修 2：缓存写入后无失效 — 用户会看到脏数据

```
当前状态：
  cache/lawyer_cache.py    ✅ 有缓存读取
  cache/redis_lock.py      ✅ 有分布式锁
  
  但缺少：律师更新资料后缓存不失效
         律所审核通过后缓存不失效
         排班变更后缓存不失效
```

```python
# ✅ 要求：在每个写操作的 Service 方法末尾添加缓存失效

# 最小改动方案 — 只需在 lawyer_cache.py 中添加失效方法

class LawyerCacheService:
    # ... 现有代码保持 ...

    async def invalidate(self, lawyer_id: str):
        """单个律师缓存失效"""
        await self._redis.delete(f"lawyer:{lawyer_id}")
        await self._redis.delete(f"lawyer:detail:{lawyer_id}")
        # 列表缓存也需要清除
        async for key in self._redis.scan_iter("lawyer:list:*"):
            await self._redis.delete(key)

    async def invalidate_by_firm(self, firm_id: str, db):
        """律所相关律师缓存批量失效"""
        stmt = select(Lawyer.id).where(Lawyer.firm_id == firm_id)
        result = await db.execute(stmt)
        for (lid,) in result:
            await self.invalidate(str(lid))

# 然后在以下位置调用：
# - LawyerService.update_lawyer()        → invalidate(lawyer_id)
# - ScheduleService.create/update()      → invalidate(lawyer_id)
# - FirmAdminService.approve_firm()      → invalidate_by_firm(firm_id)
# - InvitationService.accept()           → invalidate(lawyer_id)

# 工作量：0.5天
```

---

### 必修 3：预约超时无处理 — 排班会被永久锁死

```
当前状态：
  ✅ 预约创建时锁定排班 (Redis锁+行锁)
  ✅ 预约状态机已定义 (10个状态)
  ❌ 没有超时处理任务

致命场景：
  用户创建预约 → 排班标记 is_booked=True → 用户不支付 → 永远锁死
  该时段再也无法被其他人预约
```

```python
# ✅ 要求：在 main.py lifespan 中添加定时任务

# app/domains/appointment/timeout_task.py

class AppointmentTimeoutTask:
    """预约超时自动处理"""

    def __init__(self, session_factory, outbox_service):
        self._session_factory = session_factory
        self._outbox = outbox_service

    async def run_forever(self, shutdown_event: asyncio.Event):
        while not shutdown_event.is_set():
            try:
                await self._check_payment_timeout()
            except Exception as e:
                logger.error(f"Timeout task error: {e}")
            await asyncio.sleep(60)  # 每分钟检查一次

    async def _check_payment_timeout(self):
        """未支付超过 30 分钟的预约自动取消"""
        async with self._session_factory() as session:
            async with session.begin():
                cutoff = datetime.utcnow() - timedelta(minutes=30)
                stmt = (
                    select(LawyerConsultation)
                    .where(LawyerConsultation.status == "pending_payment")
                    .where(LawyerConsultation.created_at <= cutoff)
                    .with_for_update(skip_locked=True)
                )
                expired = (await session.execute(stmt)).scalars().all()

                for apt in expired:
                    # 1. 更新预约状态
                    apt.status = "expired"

                    # 2. 释放排班时段（关键！）
                    if apt.schedule_id:
                        schedule = await session.get(LawyerSchedule, apt.schedule_id)
                        if schedule:
                            schedule.is_booked = False

                    # 3. 通知用户
                    session.add(OutboxEvent(
                        event_type="appointment.expired",
                        payload={
                            "appointment_id": str(apt.id),
                            "user_id": str(apt.user_id),
                        }
                    ))

                if expired:
                    logger.info(f"Expired {len(expired)} unpaid appointments")

# main.py lifespan 中启动
timeout_task = AppointmentTimeoutTask(async_session, outbox_service)
timeout_shutdown = asyncio.Event()
asyncio.create_task(timeout_task.run_forever(timeout_shutdown))

# 工作量：0.5天
```

---

## 🟡 建议修复（上线后 2 周内完成）

### 建议 1：gRPC 缺少写操作

```
当前 11 个方法全是 Get/Search/Check（只读）。

需要新增的写操作（其他服务会调用）：
  - UpdateConsultationStatus    ← AI 服务分析完成后回调
  - ConfirmAppointment          ← 未来支付服务确认付款
  - CancelUserAppointments      ← 用户服务封禁/注销用户时
  - UpdateLawyerStatus          ← 用户服务律师认证回调

当前可以先用 Kafka 事件替代（已有 consumer），
但长期应补充 gRPC 写操作以支持同步调用场景。

工作量：2天
```

---

### 建议 2：跨领域级联处理

```
当前缺失的级联场景：

  律所暂停 → 旗下律师排班应取消 → 已有预约应通知用户     ❌ 未处理
  律师离开律所 → 未来排班取消 → 已有预约标记提醒          ❌ 未处理
  律师被封禁 → 所有进行中咨询关闭 → 预约取消             ❌ 未处理

建议：在对应状态机的 transition 钩子中触发 Kafka 事件，
     由 consumer 完成级联操作。

工作量：2天
```

---

### 建议 3：限流按角色差异化

```
当前 rate_limit.py 按路径限流，但未区分角色。

  普通用户每分钟预约 10 次 → 合理
  律师每分钟查询排班 10 次 → 太少了
  管理员每分钟审批 10 次   → 太少了

建议：在限流中间件中读取 user.role，按 (path, role) 查表限流。

工作量：0.5天
```

---

### 建议 4：Outbox 健康监控

```
当前 Outbox 只有写入和发送，没有监控。

  如果 Outbox publisher 卡住：
  - pending 事件堆积 → 无人知晓
  - 事件延迟从秒级恶化到小时级 → 无告警

建议：
  1. /health/ready 中检查 stale 事件数
  2. /metrics 中暴露 outbox_pending_count gauge
  3. stale > 50 时 log.critical

工作量：0.5天
```

---

### 建议 5：补充关键测试

```
当前测试覆盖评估：

  ✅ 有单元测试
  ❌ 缺少状态机转换测试（4 个状态机的非法转换是否正确拒绝？）
  ❌ 缺少权限矩阵测试（34 个权限 × 5 个角色是否正确映射？）
  ❌ 缺少预约并发测试（两人同时预约同一时段是否只成功一个？）
  ❌ 缺少律所入驻端到端测试

建议优先补充的 4 个测试文件：
  tests/test_state_machines.py      ← 所有状态机合法/非法转换
  tests/test_permissions.py         ← 角色权限矩阵
  tests/test_appointment_concurrency.py  ← 并发预约
  tests/test_firm_onboarding_e2e.py     ← 入驻流程端到端

工作量：3天
```

---

## 🔵 可延后项（按迭代推进）

| 项 | 说明 | 建议时间 |
|---|------|---------|
| 批量操作接口 | 管理员批量审批/批量查询 | 下个迭代 |
| 状态机数据库约束 | 当前是应用层校验，可加 DB CHECK | 按需 |
| 权限与用户服务打通 | 当前本地映射，未来可走 gRPC 查权限 | 用户服务改 RS256 后 |
| 服务进一步拆分 | 87 端点拆为 2-3 个服务 | 3-6 个月后视团队规模 |

---

## 📋 验收前必做清单

```
┌──┬─────────────────────────────────────────┬──────┬──────┐
│  │ 事项                                    │ 工作量│ 状态 │
├──┼─────────────────────────────────────────┼──────┼──────┤
│ 1│ 完成全部 7 个领域的目录隔离               │ 1.5天│  ☐  │
│ 2│ 添加缓存写后失效（5 个调用点）            │ 0.5天│  ☐  │
│ 3│ 添加预约超时自动处理 + 排班释放           │ 0.5天│  ☐  │
├──┼─────────────────────────────────────────┼──────┼──────┤
│  │ 合计                                    │ 2.5天│      │
└──┴─────────────────────────────────────────┴──────┴──────┘
```

---

## 🆚 回答你提出的 4 个问题

### Q1: 状态机是否需要数据库层约束？

```
回答：当前阶段不需要。

原因：
  应用层状态机已经覆盖了所有入口（Service + gRPC）。
  数据库 CHECK 约束只能限制值范围，无法限制转换方向。
  真正的防线是应用层的 can_transition() 检查。

  只需确保：所有写入状态的代码都经过状态机，不要直接 UPDATE status。

  如果未来担心绕过应用层（如 DBA 直接改库），可以加 PostgreSQL 触发器，
  但那是过度防御，当前不需要。
```

### Q2: 领域隔离是否需要扩展到其他领域？

```
回答：是的，必须扩展。这是上面必修项 1。

  只隔离 firm/ 一个领域，其余保持扁平，是最危险的状态——
  两种风格并存会快速退化。要么全做，要么不做。
```

### Q3: 权限系统是否需要与用户服务打通？

```
回答：当前阶段不需要，但需要预留接口。

  当前方案：JWT Token 中携带 role → 本地 ROLE_PERMISSIONS 映射
  这个方案在以下条件下是充分的：
    1. 角色数量少（5 个）
    2. 权限变更频率低
    3. JWT 中的 role 字段由 user-service 签发

  未来打通方案（当用户服务切换到 RS256 后）：
    - user-service 签发 Token 时把 permissions 列表写入 JWT claims
    - 或者 legal-service 通过 gRPC 调用 user-service.CheckPermission()
    - 当前的 Permission enum 设计已经兼容这两种方案

  不需要现在做。
```

### Q4: gRPC 是否需要增加写操作？

```
回答：需要，但不阻塞验收。

  当前跨服务写操作通过 Kafka 事件异步完成，功能上不缺失。
  gRPC 写操作是为了支持"需要同步确认结果"的场景（如支付确认）。
  可以在上线后 2 周内补充。
```

---

## 🎯 最终结论

```
┌──────────────────────────────────────────────────────────┐
│                                                          │
│  修复 3 项必修内容（2.5 天）后，律师服务可以正式验收上线。  │
│                                                          │
│  服务已具备：                                             │
│  ✅ 完整的业务功能（87 端点覆盖全场景）                     │
│  ✅ 企业级基础设施（Outbox/幂等/限流/缓存/gRPC）           │
│  ✅ 业务治理能力（4 状态机 + 34 权限）                     │
│  ✅ 可观测性（Prometheus + OTEL + 结构化日志）              │
│  ✅ 可靠性保障（事务管理 + 分布式锁 + 优雅关闭）            │
│                                                          │
│  在整个百姓法律助手平台中，律师服务是最成熟的微服务。        │
│                                                          │
└──────────────────────────────────────────────────────────┘
```