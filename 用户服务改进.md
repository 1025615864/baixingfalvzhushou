# 用户服务（User Service）— 不足分析与完善计划

---

## 一、当前完成度评估

```
┌──────────────────────────────────────────────────────────────────────┐
│                    用户服务成熟度雷达                                  │
│                                                                      │
│                  核心功能                                             │
│                 ██████████ 9/10                                      │
│                ╱          ╲                                          │
│   安全加固    ╱            ╲     服务通信                             │
│  ███████ 7/10              8/10 ████████                             │
│            ╲              ╱                                          │
│             ╲            ╱                                           │
│  生产就绪    ╲          ╱    基础设施集成                             │
│  █████ 5/10   ╲        ╱  6/10 ██████                               │
│                ╲      ╱                                              │
│               测试覆盖                                               │
│              ████ 4/10                                               │
└──────────────────────────────────────────────────────────────────────┘
```

核心业务功能做得很扎实，但在**安全纵深**、**生产加固**、**基础设施集成**、**数据一致性**方面存在明显短板。

---

## 二、问题全景分类

```
┌──────────────────────────────────────────────────────────────────────┐
│                      问题严重度层级                                    │
│                                                                      │
│                     🔴 安全隐患                                       │
│                 ┌──────────────────┐                                 │
│                 │ JWT HS256 单密钥   │                                │
│                 │ 密码策略无校验     │                                │
│                 │ 敏感数据明文日志   │                                │
│                 │ 无操作审计日志     │                                │
│                 └──────────────────┘                                 │
│               🟠 架构 / 基础设施缺口                                  │
│           ┌──────────────────────────┐                               │
│           │ common组件未完全集成      │                               │
│           │ 无Alembic迁移            │                               │
│           │ 无健康检查端点            │                               │
│           │ Kafka Consumer缺失       │                               │
│           │ 用户数据变更不广播        │                               │
│           └──────────────────────────┘                               │
│             🟡 功能 / 质量缺失                                        │
│         ┌──────────────────────────────┐                             │
│         │ 无手机/邮箱验证流程          │                             │
│         │ 无OAuth2社会化登录           │                             │
│         │ 无密码重置流程               │                             │
│         │ 无用户头像上传               │                             │
│         │ 无单元/集成测试              │                             │
│         └──────────────────────────────┘                             │
│               ⚪ 运营 / 体验增强                                      │
│           ┌──────────────────────────────┐                           │
│           │ 无用户行为分析              │                             │
│           │ 无管理后台统计              │                             │
│           │ 无账号注销/数据导出(合规)    │                             │
│           └──────────────────────────────┘                           │
└──────────────────────────────────────────────────────────────────────┘
```

---

## 三、逐项深度分析

---

### 🔴 模块一：安全纵深加固（最高优先级）

用户服务是**整个系统的安全根基**，任何安全缺陷都会波及所有微服务。

#### 1.1 JWT 算法升级

```
当前:  HS256 (对称加密) — 所有服务共享同一个 SECRET_KEY
问题:  任何拿到 SECRET_KEY 的服务都能伪造 Token
       密钥泄露 = 全系统沦陷
```

```
目标:  RS256 (非对称加密) — 私钥签发，公钥验证

┌──────────────┐                    ┌──────────────────┐
│ User Service │                    │  其他所有服务      │
│              │                    │                  │
│ 持有私钥      │    JWT Token       │  仅持有公钥       │
│ (签发Token)  │ ──────────────▶    │  (只能验证        │
│              │                    │   不能伪造)       │
└──────────────┘                    └──────────────────┘
```

#### 1.2 密码安全策略

```python
# 当前: 注册时无密码强度校验，任何字符串都能注册
# 需要:
class PasswordPolicy:
    MIN_LENGTH = 8
    REQUIRE_UPPERCASE = True
    REQUIRE_LOWERCASE = True  
    REQUIRE_DIGIT = True
    REQUIRE_SPECIAL = False         # 法律平台用户群体偏大众，不宜太严格
    COMMON_PASSWORD_CHECK = True    # 拒绝 "123456", "password" 等
    
    def validate(self, password: str) -> tuple[bool, str]:
        if len(password) < self.MIN_LENGTH:
            return False, f"密码长度不能少于{self.MIN_LENGTH}位"
        if self.REQUIRE_UPPERCASE and not re.search(r'[A-Z]', password):
            return False, "密码需要包含大写字母"
        # ...
```

#### 1.3 敏感数据保护

```
需要检查和修复:
├── 日志中不能输出 password, token, JWT secret
├── API响应中永远不返回 hashed_password 字段
├── 错误信息不能泄露用户是否存在
│   ❌ "用户名不存在"  → 攻击者可枚举用户
│   ✅ "用户名或密码错误" → 统一提示
└── Redis中存储的Token相关数据设置合理TTL
```

#### 1.4 操作审计日志

```
法律平台合规要求：关键操作必须留痕

需要记录:
├── 登录成功/失败 (IP, User-Agent, 时间)
├── 密码修改
├── 会员等级变更
├── 权限变更
├── 账号删除
├── Token刷新
└── 异常行为 (频繁登录失败, 异地登录)

存储: 独立的 audit_log 表，不可修改，仅追加
```

| # | 任务 | 工时 |
|---|------|------|
| 1.1 | JWT HS256 → RS256 升级 | 1.5天 |
| 1.2 | 密码强度校验策略 | 0.5天 |
| 1.3 | 敏感数据脱敏 (日志 + 响应) | 0.5天 |
| 1.4 | 登录错误信息统一化 | 0.5天 |
| 1.5 | 操作审计日志表 + 记录 | 1天 |
| 1.6 | Token Refresh 轮换策略 (Rotation) | 1天 |

**1.6 Token Rotation 说明**：

```
当前:  Refresh Token 可以无限次使用，被盗后攻击者可持续刷新
目标:  每次 Refresh 后旧 Token 立即失效 (Rotation)

Request:  POST /refresh  { refresh_token: "old_token" }
Response: { access_token: "new_access", refresh_token: "new_refresh" }
          ← "old_token" 立即加入黑名单

如果旧 Token 被再次使用 → 说明被盗 → 撤销该用户所有 Token
```

---

### 🟠 模块二：common 公共组件集成

对照其他已集成的服务（Backend, Legal, Order），User Service 缺少以下集成：

```
┌──────────────────────────────────────────────────────────────────────┐
│             User Service ← common 集成检查                           │
├──────────────────────┬────────────┬──────────────────────────────────┤
│       组件            │  当前状态   │       说明                       │
├──────────────────────┼────────────┼──────────────────────────────────┤
│ OpenTelemetry 追踪   │    ⚠️      │ 需确认是否在 main.py 初始化      │
│ Consul 服务注册      │    ⚠️      │ 需确认生命周期注册/注销           │
│ TracingKafkaProducer │    ⚠️      │ 是否使用带追踪的 Producer        │
│ 统一 CORS 配置       │    ⚠️      │ 是否调用 get_cors_config()       │
│ ConfigManager        │    ⚠️      │ 是否使用统一配置管理              │
│ 健康检查端点          │    ❌      │ 缺少 /health /health/ready      │
│ 统一错误码           │    ❌      │ 散落的 HTTPException             │
│ 结构化日志           │    ⚠️      │ 需确认 JSON 格式 + trace_id     │
└──────────────────────┴────────────┴──────────────────────────────────┘
```

**健康检查对 User Service 特别重要**——因为其他所有服务都依赖它验证 Token：

```python
@router.get("/health/ready")
async def readiness():
    checks = {}
    
    # 数据库 — 核心依赖
    try:
        await db.execute(text("SELECT 1"))
        checks["database"] = "ok"
    except Exception:
        checks["database"] = "fail"
    
    # Redis — Token黑名单依赖
    try:
        await redis.ping()
        checks["redis"] = "ok"
    except Exception:
        checks["redis"] = "fail"
    
    # gRPC Server — 其他服务依赖
    checks["grpc"] = "ok" if grpc_server.is_serving() else "fail"
    
    all_ok = all(v == "ok" for v in checks.values())
    status_code = 200 if all_ok else 503
    
    return JSONResponse(
        status_code=status_code,
        content={
            "status": "ready" if all_ok else "degraded",
            "checks": checks,
            "dependencies_count": len(checks)
        }
    )
```

| # | 任务 | 工时 |
|---|------|------|
| 2.1 | 健康检查三端点 | 0.5天 |
| 2.2 | 确认/补全 OpenTelemetry 初始化 | 0.5天 |
| 2.3 | 确认/补全 Consul 注册 | 0.5天 |
| 2.4 | TracingKafkaProducer 替换 | 0.5天 |
| 2.5 | 统一错误码体系 | 1天 |
| 2.6 | Dockerfile | 0.5天 |

---

### 🟠 模块三：数据变更事件广播（生态闭环关键）

**这是当前最被忽视但影响最大的问题。**

User Service 是**数据源头**，但用户数据变更后**不通知其他服务**，导致整个微服务架构中数据不一致。

```
当前问题:

用户改名 "张三" → "李四"
    │
    ├── User Service 数据库: ✅ 已更新
    │
    ├── Community Service 帖子表 author_name: ❌ 仍然是 "张三"
    ├── Community Service 评论表 author_name: ❌ 仍然是 "张三"
    ├── Legal Service 咨询记录:              ❌ 仍然是 "张三"
    └── Order Service 订单记录:              ❌ 仍然是 "张三"
```

**需要广播的事件清单**：

```
┌────────────────────────────────┬─────────────────────────────────────┐
│           事件类型               │            消费者                    │
├────────────────────────────────┼─────────────────────────────────────┤
│ user.profile.updated           │ Community (更新冗余字段)             │
│  {user_id, nickname, avatar}   │ Legal (更新咨询记录)                │
│                                │ Order (更新订单记录)                │
├────────────────────────────────┼─────────────────────────────────────┤
│ user.lawyer.verified           │ Community (更新律师标识)             │
│  {user_id, is_lawyer: true}    │ Legal (更新律师状态)                │
├────────────────────────────────┼─────────────────────────────────────┤
│ user.membership.upgraded       │ Points (会员权益变更)               │
│  {user_id, old_level, new}     │ Notification (恭喜通知)             │
│                                │ AI Service (调整配额)               │
├────────────────────────────────┼─────────────────────────────────────┤
│ user.membership.expired        │ Notification (续费提醒)             │
│  {user_id, expired_level}      │ AI Service (降级配额)               │
├────────────────────────────────┼─────────────────────────────────────┤
│ user.account.deleted           │ Community (匿名化帖子)              │
│  {user_id}                     │ Legal (匿名化记录)                  │
│                                │ AI (清除会话)                       │
├────────────────────────────────┼─────────────────────────────────────┤
│ user.account.banned            │ Community (隐藏帖子)                │
│  {user_id, reason}             │ Notification (通知)                 │
├────────────────────────────────┼─────────────────────────────────────┤
│ user.login.success             │ Points (+1登录积分)                 │
│  {user_id, ip, device}         │ 已有 ✅                             │
├────────────────────────────────┼─────────────────────────────────────┤
│ user.registered                │ Points (+10注册积分)                │
│  {user_id}                     │ Notification (欢迎)  已有 ✅        │
└────────────────────────────────┴─────────────────────────────────────┘
```

**核心实现**：

```python
# 在 user_service.py 的 update_user 方法中补充事件发布
async def update_user(self, user_id: int, update_data: dict):
    user = await self._get_user(user_id)
    
    # 记录变更前的值（用于事件）
    old_nickname = user.nickname
    old_avatar = user.avatar
    
    # 更新数据库
    for key, value in update_data.items():
        setattr(user, key, value)
    await self.db.commit()
    
    # 发布变更事件（仅当关键字段变更时）
    if update_data.get("nickname") != old_nickname or \
       update_data.get("avatar") != old_avatar:
        await self.event_bus.publish(UserEvent(
            event_type="user.profile.updated",
            data={
                "user_id": user_id,
                "nickname": user.nickname,
                "avatar": user.avatar,
                "changed_fields": list(update_data.keys())
            }
        ))
    
    return user
```

| # | 任务 | 工时 |
|---|------|------|
| 3.1 | user.profile.updated 事件发布 | 0.5天 |
| 3.2 | user.membership.upgraded/expired 事件 | 0.5天 |
| 3.3 | user.account.deleted/banned 事件 | 0.5天 |
| 3.4 | user.lawyer.verified 事件 | 0.5天 |
| 3.5 | 事件发布点集成到所有 Service 方法 | 1天 |

---

### 🟠 模块四：数据库生产就绪

```
当前:
├── SQLite (开发)  ← 不支持并发写入，不适合 gRPC 多连接
├── 无 Alembic    ← 无法追踪 schema 变更
└── datetime.utcnow (可能)  ← Python 3.12+ 废弃

需要:
├── PostgreSQL 配置模板（生产）
├── Alembic 迁移脚本
├── 连接池配置 (asyncpg + pool_size)
├── 读写分离准备 (可选)
└── 数据库索引审查
```

**关键索引检查**：

```sql
-- 用户服务需要的核心索引（确认是否存在）

-- 登录查询: 按用户名查找
CREATE INDEX idx_users_username ON users(username);

-- 邮箱查询: 按邮箱查找
CREATE UNIQUE INDEX idx_users_email ON users(email);

-- 手机查询: 按手机号查找
CREATE UNIQUE INDEX idx_users_phone ON users(phone);

-- Token 验证: 按 user_id 查关联数据
CREATE INDEX idx_user_sessions_user_id ON user_sessions(user_id);

-- 会员查询: 按等级筛选
CREATE INDEX idx_users_membership_level ON users(membership_level);

-- 审计日志: 按时间范围查询
CREATE INDEX idx_audit_logs_created_at ON audit_logs(created_at);
CREATE INDEX idx_audit_logs_user_id ON audit_logs(user_id);
```

| # | 任务 | 工时 |
|---|------|------|
| 4.1 | Alembic 初始化 + 初始迁移 | 1天 |
| 4.2 | PostgreSQL 连接池配置 | 0.5天 |
| 4.3 | 数据库索引审查 + 补充 | 0.5天 |
| 4.4 | datetime 修复 | 0.5天 |

---

### 🟡 模块五：缺失的用户功能

```
┌──────────────────────────────────────────────────────────────────────┐
│                    用户功能完整性对照                                  │
│                                                                      │
│  基础认证              高级认证              用户管理                  │
│  ✅ 用户名密码登录      ❌ 手机验证码登录      ✅ CRUD                 │
│  ✅ Token 刷新          ❌ 邮箱验证            ✅ 会员等级             │
│  ✅ 频率限制            ❌ 密码重置            ❌ 头像上传             │
│  ✅ Token 撤销          ❌ OAuth2 社会化登录   ❌ 账号注销             │
│                        ❌ 双因素认证(2FA)     ❌ 数据导出             │
│                                              ❌ 登录设备管理         │
└──────────────────────────────────────────────────────────────────────┘
```

**按优先级排列**：

#### 5.1 密码重置流程（高优先级）

```
用户忘记密码 → 点击"忘记密码"
    │
    ▼
┌──────────────────────────────────────────────┐
│  方案A: 邮箱重置                              │
│  1. 输入注册邮箱                              │
│  2. 发送重置链接 (含一次性Token, 15分钟过期)    │
│  3. 点击链接 → 输入新密码                      │
│  4. 旧密码作废 + 所有Token撤销                 │
│                                              │
│  方案B: 手机验证码重置                         │
│  1. 输入注册手机号                             │
│  2. 发送短信验证码 (6位, 5分钟过期)             │
│  3. 输入验证码 + 新密码                        │
│  4. 旧密码作废 + 所有Token撤销                 │
└──────────────────────────────────────────────┘
```

#### 5.2 账号注销 + 数据导出（合规要求）

```
中国《个人信息保护法》要求:
├── 用户有权注销账号
├── 用户有权导出个人数据
└── 注销后数据保留期不超过规定期限

注销流程:
1. 用户申请注销 → 进入冷静期 (15天)
2. 冷静期内可取消
3. 冷静期后 → 软删除用户数据
4. 发布 user.account.deleted 事件
5. 其他服务匿名化关联数据
6. 30天后硬删除
```

#### 5.3 登录设备管理

```
法律平台用户可能涉及敏感法律咨询，设备管理很重要:

GET /api/v1/users/me/devices
[
    {
        "device_id": "xxx",
        "device_name": "iPhone 15 Pro",
        "ip": "192.168.1.1",
        "location": "北京",
        "last_active": "2026-03-22T10:00:00Z",
        "is_current": true
    },
    {
        "device_id": "yyy",
        "device_name": "Windows Chrome",
        "ip": "10.0.0.1",
        "location": "上海",
        "last_active": "2026-03-21T15:00:00Z",
        "is_current": false
    }
]

DELETE /api/v1/users/me/devices/{device_id}   ← 远程登出
DELETE /api/v1/users/me/devices               ← 登出所有设备
```

| # | 任务 | 工时 |
|---|------|------|
| 5.1 | 密码重置流程 (邮箱) | 1.5天 |
| 5.2 | 邮箱验证流程 | 1天 |
| 5.3 | 账号注销 + 冷静期 | 1.5天 |
| 5.4 | 个人数据导出 API | 1天 |
| 5.5 | 登录设备管理 | 1天 |
| 5.6 | 头像上传 (对象存储) | 1天 |
| 5.7 | OAuth2 微信登录 (可选) | 2天 |

---

### 🟡 模块六：测试体系

```
当前: 无任何测试 ❌

用户服务是安全核心，测试覆盖必须最高:

tests/
├── unit/
│   ├── test_auth_service.py       # 登录/注册/Token逻辑
│   ├── test_token_service.py      # Token撤销/轮换/黑名单
│   ├── test_password_policy.py    # 密码强度校验
│   ├── test_membership_service.py # 会员等级/权限
│   └── test_user_service.py       # 用户CRUD
│
├── integration/
│   ├── test_auth_api.py           # 完整登录/注册流程
│   ├── test_token_refresh.py      # Token刷新+轮换
│   ├── test_rate_limiting.py      # 登录频率限制
│   └── test_grpc_server.py        # gRPC接口测试
│
├── security/
│   ├── test_sql_injection.py      # SQL注入测试
│   ├── test_jwt_tampering.py      # JWT篡改测试
│   ├── test_brute_force.py        # 暴力破解测试
│   └── test_sensitive_data.py     # 敏感数据泄露测试
│
└── contract/
    └── test_user_provider.py      # Pact Provider测试
                                   # (Community/Legal/AI 依赖的契约)
```

**安全测试尤其重要**：

```python
# tests/security/test_jwt_tampering.py
class TestJWTSecurity:
    
    async def test_expired_token_rejected(self):
        """过期Token必须被拒绝"""
        
    async def test_invalid_signature_rejected(self):
        """篡改签名的Token必须被拒绝"""
        
    async def test_none_algorithm_rejected(self):
        """alg: none 攻击必须被拒绝"""
        
    async def test_token_from_different_secret_rejected(self):
        """不同密钥签发的Token必须被拒绝"""
        
    async def test_revoked_token_rejected(self):
        """已撤销的Token必须被拒绝"""

# tests/security/test_brute_force.py
class TestBruteForceProtection:
    
    async def test_lockout_after_5_failures(self):
        """5次失败后账号锁定15分钟"""
        
    async def test_lockout_returns_same_error(self):
        """锁定状态下不泄露账号是否存在"""
        
    async def test_successful_login_resets_counter(self):
        """成功登录后计数器重置"""
```

| # | 任务 | 工时 |
|---|------|------|
| 6.1 | Auth Service 单元测试 | 1天 |
| 6.2 | Token Service 单元测试 | 1天 |
| 6.3 | API 集成测试 | 1.5天 |
| 6.4 | 安全测试 (JWT + 暴力破解) | 1天 |
| 6.5 | Pact Provider 契约测试 | 1天 |

---

### ⚪ 模块七：gRPC 服务增强

```
当前 gRPC 方法:
├── GetUser              ✅
├── GetUserByUsername     ✅
├── ValidateToken         ✅
├── CheckPermission       ✅
├── GetUserProfile        ✅
└── UpdateUserProfile     ✅

缺少但其他服务需要的:
├── BatchGetUsers         ← Community批量获取帖子作者信息
├── GetUsersByIds         ← 同上，批量查询
├── ValidateTokenBatch    ← 网关批量验证 (可选)
├── GetUserStats          ← 管理后台统计
└── SubscribeUserChanges  ← gRPC Stream 实时推送变更 (高级)
```

**BatchGetUsers 的必要性**：

```
当前:  社区帖子列表 → 每个帖子的 author 信息需要单独调用 GetUser
       20条帖子 = 20次 gRPC 调用  ← N+1 问题

优化:  一次 BatchGetUsers({user_ids: [1,2,3,...20]})
       20条帖子 = 1次 gRPC 调用
```

| # | 任务 | 工时 |
|---|------|------|
| 7.1 | BatchGetUsers gRPC 方法 | 1天 |
| 7.2 | Proto 定义更新 + 编译 | 0.5天 |
| 7.3 | gRPC 健康检查 (grpc.health.v1) | 0.5天 |

---

## 四、实施路线图

```
Phase 1 (第1-2周) ━━ 安全加固 + 基础设施 ━━━━━━━━━━━ 🔴 最紧急
│
├── Week 1 — 安全
│   ├── 1.2  密码强度校验                          0.5天
│   ├── 1.3  敏感数据脱敏                          0.5天
│   ├── 1.4  登录错误信息统一                       0.5天
│   ├── 1.5  操作审计日志                          1天
│   ├── 1.6  Token Refresh Rotation               1天
│   └── 2.1  健康检查端点                          0.5天
│
├── Week 2 — 基础设施
│   ├── 1.1  JWT RS256 升级                        1.5天
│   ├── 2.2-2.4  common组件确认/补全               1天
│   ├── 2.5  统一错误码                            1天
│   └── 4.1  Alembic 迁移                         1天
│
Phase 2 (第3-4周) ━━ 数据一致性 + 核心功能 ━━━━━━━━ 🟠
│
├── Week 3
│   ├── 3.1-3.5  事件广播全量补全                   3天
│   ├── 5.1  密码重置流程                          1.5天
│   └── 4.3  数据库索引审查                         0.5天
│
├── Week 4
│   ├── 5.3  账号注销 + 冷静期                      1.5天
│   ├── 5.4  个人数据导出                          1天
│   ├── 7.1  BatchGetUsers                         1天
│   └── 2.6  Dockerfile                            0.5天
│
Phase 3 (第5-6周) ━━ 测试 + 增强功能 ━━━━━━━━━━━━━ 🟡
│
├── Week 5
│   ├── 6.1-6.4  测试体系建设                      4.5天
│
├── Week 6
│   ├── 5.2  邮箱验证                              1天
│   ├── 5.5  登录设备管理                          1天
│   ├── 5.6  头像上传                              1天
│   └── 6.5  契约测试                              1天
```

---

## 五、完整任务总表

| # | 任务 | 优先级 | Phase | 工时 |
|---|------|--------|-------|------|
| 1.1 | JWT HS256 → RS256 | 🔴 | 1 | 1.5天 |
| 1.2 | 密码强度校验 | 🔴 | 1 | 0.5天 |
| 1.3 | 敏感数据脱敏 | 🔴 | 1 | 0.5天 |
| 1.4 | 登录错误统一化 | 🔴 | 1 | 0.5天 |
| 1.5 | 操作审计日志 | 🔴 | 1 | 1天 |
| 1.6 | Token Rotation | 🔴 | 1 | 1天 |
| 2.1 | 健康检查端点 | 🔴 | 1 | 0.5天 |
| 2.2 | OpenTelemetry 确认 | 🟠 | 1 | 0.5天 |
| 2.3 | Consul 确认 | 🟠 | 1 | 0.5天 |
| 2.4 | TracingProducer | 🟠 | 1 | 0.5天 |
| 2.5 | 统一错误码 | 🟠 | 1 | 1天 |
| 2.6 | Dockerfile | 🟠 | 2 | 0.5天 |
| 3.1 | user.profile.updated 事件 | 🟠 | 2 | 0.5天 |
| 3.2 | user.membership 事件 | 🟠 | 2 | 0.5天 |
| 3.3 | user.account 事件 | 🟠 | 2 | 0.5天 |
| 3.4 | user.lawyer.verified 事件 | 🟠 | 2 | 0.5天 |
| 3.5 | 事件发布集成到所有方法 | 🟠 | 2 | 1天 |
| 4.1 | Alembic 迁移 | 🟠 | 1 | 1天 |
| 4.2 | PostgreSQL 连接池 | 🟡 | 2 | 0.5天 |
| 4.3 | 索引审查 | 🟡 | 2 | 0.5天 |
| 4.4 | datetime 修复 | 🟠 | 1 | 0.5天 |
| 5.1 | 密码重置 | 🟡 | 2 | 1.5天 |
| 5.2 | 邮箱验证 | 🟡 | 3 | 1天 |
| 5.3 | 账号注销 + 冷静期 | 🟡 | 2 | 1.5天 |
| 5.4 | 数据导出 | 🟡 | 2 | 1天 |
| 5.5 | 登录设备管理 | ⚪ | 3 | 1天 |
| 5.6 | 头像上传 | ⚪ | 3 | 1天 |
| 5.7 | OAuth2 微信登录 | ⚪ | 4 | 2天 |
| 6.1 | Auth 单元测试 | 🟡 | 3 | 1天 |
| 6.2 | Token 单元测试 | 🟡 | 3 | 1天 |
| 6.3 | API 集成测试 | 🟡 | 3 | 1.5天 |
| 6.4 | 安全测试 | 🟡 | 3 | 1天 |
| 6.5 | Pact 契约测试 | ⚪ | 3 | 1天 |
| 7.1 | BatchGetUsers | 🟡 | 2 | 1天 |
| 7.2 | Proto 更新 | 🟡 | 2 | 0.5天 |
| 7.3 | gRPC 健康检查 | ⚪ | 3 | 0.5天 |

**总计：约 32 个工作日（6-7 周）**

---

## 六、核心判断

```
如果你只有 1 周 → Phase 1 前半 (安全加固 1.2-1.6 + 2.1)
    效果: 堵住安全漏洞，用户服务不再是系统最薄弱环节

如果你有 2 周  → 完成 Phase 1
    效果: 安全 + 基础设施集成完成，可安全部署

如果你有 3 周  → Phase 1 + 2 (含事件广播)
    效果: 与其他微服务形成数据一致性闭环

如果你有 4+ 周 → 全部
    效果: 生产级用户服务
```

**一句话总结：用户服务的业务功能做得很好，但作为整个系统的安全根基，安全纵深和数据变更广播是最紧迫的两个缺口。Phase 1 的安全加固是保命措施，Phase 2 的事件广播是让微服务架构真正"活"起来的关键。**
