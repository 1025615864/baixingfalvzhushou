# 用户服务（User Service）验收评审报告

---

## 🏆 总评

```
┌───────────────────────────────────────────────────────────────┐
│                                                               │
│   验收结论：  ✅ 有条件通过验收                                │
│   成熟度：    92%                                              │
│   评级：      Production Ready (with 2 minor fixes)           │
│                                                               │
│   必须修复项：  2 项（预计 1 天）                               │
│   优化建议：    4 项（不阻塞上线）                              │
│                                                               │
└───────────────────────────────────────────────────────────────┘
```

---

## 📊 历史问题修复追踪

| 首轮提出的问题 | 当前状态 | 验证 |
|---------------|---------|------|
| 🔴 用户 ID 自增 int (IDOR) | ✅ **已修复** | User 模型增加 `uid (UUID)`，API 对外使用 UUID |
| 🔴 User/Profile 字段重复 | ✅ **已修复** | User 只保留认证字段，nickname/avatar 移至 Profile |
| 🔴 gRPC 端口与 Legal 冲突 | ✅ **已修复** | User=50051, Legal=50052 |
| 🔴 JWT HS256 密钥共享 | ✅ **已修复** | RS256 非对称加密 + JWKS 端点分发公钥 |
| 🔴 Refresh Token 无轮换 | ✅ **已修复** | Rotation + 重放检测，旧 Token 立即失效 |
| 🟡 设备数无上限 | ✅ **已修复** | 最多 5 设备 |
| 🟡 LoginAudit/AuditLog 重叠 | ✅ **已修复** | 合并为统一 AuditLog |
| 🟡 注册接口无防刷 | ⚠️ **待确认** | 有登录限流，注册限流待确认 |
| 🟡 会员过期无自动处理 | ⚠️ **待确认** | 有 expired 事件定义，触发机制待确认 |
| 🟡 Kafka 默认关闭 | ⚠️ **仍存在** | `ENABLE_KAFKA_CONSUMER=false` |
| 🟡 GDPR 导出同步阻塞 | ⚠️ **待确认** | 端点存在，异步化待确认 |

**核心安全问题修复率：5/5 = 100% ✅ — 认证架构已达企业级水准**

---

## ✅ 验收通过项（无需修改）

### 1. 认证架构 — 全平台最强 🏆

```
✅ RS256 非对称加密
   → 用户服务持有私钥（签发）
   → 其他服务只需公钥（验证）
   → 任何服务泄露公钥也无法伪造 Token

✅ JWKS 端点 (/.well-known/jwks.json)
   → 其他服务自动获取公钥
   → 密钥轮换时无需手动更新各服务

✅ Refresh Token Rotation + 重放检测
   → 每次刷新生成新 Token 对
   → 旧 Refresh Token 立即失效
   → 检测到重放攻击时吊销整个 Token 家族

评价：认证方案已达金融级安全标准
```

### 2. 用户模型设计 — 干净清晰

```
✅ User: 认证核心字段 (uid/phone/password/role/status/membership)
✅ Profile: 展示信息 (nickname/avatar/bio/verified)
✅ Device: 设备管理 (max 5, 远程登出)
✅ AuditLog: 统一审计 (登录/改密/会员/角色)

无冗余字段、无重叠表、FK 关系清晰
```

### 3. API 设计 — 规范合理

```
✅ 27 个端点，职责清晰
✅ RESTful 规范（GET/POST/PUT/DELETE 语义正确）
✅ 认证标注清楚（公开 vs 登录）
✅ 路由按业务模块拆分（auth/user/profile/membership/account/device）
✅ 无冗余端点
```

### 4. 安全纵深 — 多层防御

```
✅ 登录限流：5 次失败锁 15 分钟
✅ 密码策略：长度 ≥ 8 + 大小写 + 数字 + 弱密码检查
✅ 敏感数据脱敏：日志过滤 password/token
✅ 防枚举攻击：统一错误信息
✅ 设备管理：上限 5 台 + 远程登出
✅ 审计日志：全操作记录
```

### 5. gRPC 接口 — 完整且实用

```
✅ GetUser / GetUserByPhone / GetUserByUID — 三种查询方式
✅ BatchGetUsers — 批量查询（律师列表页展示创建者）
✅ VerifyToken — 其他服务验证 JWT
✅ GetUserRole — 权限检查

6 个方法覆盖了所有跨服务调用场景
```

### 6. 测试覆盖 — 全平台最全面

```
✅ test_auth_service.py      — 注册/登录逻辑
✅ test_token_service.py     — Token 生命周期
✅ test_security.py          — 安全攻防测试
✅ test_api.py               — API 集成测试
✅ test_token_refresh.py     — Rotation 专项测试
✅ test_rate_limiting.py     — 限流有效性测试
✅ test_pact_provider.py     — 跨服务契约测试 ← 全平台唯一

7 个测试文件，覆盖单元/集成/安全/契约 4 个层次
```

---

## 🔴 必须修复（预计 1 天）

### 必修 1：Kafka 消费者默认关闭 + 事件发布无 Fallback

```
当前配置：
  ENABLE_KAFKA_CONSUMER = false     ← 默认关闭
  ENABLE_KAFKA_PRODUCER = ?         ← 可能也有开关

问题链：

  场景 1：Kafka 未启用
  用户注册 → 发送 user.registered 事件 → Kafka 关闭 → 事件丢失
  → AI 服务不知道有新用户 → 新用户的 AI 对话配额未初始化

  场景 2：Kafka 短暂宕机
  用户升级会员 → user.membership.upgraded → Kafka 不可用 → 事件丢失
  → AI 服务仍用旧配额 → 用户付费后体验不变 → 投诉

  Legal Service 已有 Outbox 模式，User Service 反而没有。
  作为全平台的认证中心，User 的事件可靠性要求更高。
```

```python
# ✅ 方案：添加 Outbox 表 + Fallback

# 1. 新增 Outbox 模型
# app/models/outbox.py
class OutboxEvent(Base):
    __tablename__ = "outbox_events"

    id = Column(UUID, primary_key=True, default=uuid4)
    event_type = Column(String(100), nullable=False, index=True)
    payload = Column(JSON, nullable=False)
    status = Column(String(20), default="pending", index=True)
    retry_count = Column(Integer, default=0)
    created_at = Column(DateTime, server_default=func.now())
    published_at = Column(DateTime, nullable=True)

# 2. 事件发布改为 Outbox 模式
class EventPublisher:
    async def publish(self, event_type: str, payload: dict, db: AsyncSession):
        """事件先写入 Outbox（与业务在同一事务），再异步发送到 Kafka"""
        outbox = OutboxEvent(
            event_type=event_type,
            payload=payload,
            status="pending",
        )
        db.add(outbox)
        # 不立即发 Kafka，由后台任务扫描发送

    async def flush_outbox(self, db: AsyncSession):
        """后台任务：扫描 Outbox 发送到 Kafka"""
        events = await db.execute(
            select(OutboxEvent)
            .where(OutboxEvent.status == "pending")
            .where(OutboxEvent.retry_count < 5)
            .order_by(OutboxEvent.created_at)
            .limit(100)
            .with_for_update(skip_locked=True)
        )
        for event in events.scalars():
            try:
                if self._kafka_enabled:
                    await self._kafka.send(
                        f"user.{event.event_type}",
                        value=json.dumps(event.payload).encode()
                    )
                event.status = "published"
                event.published_at = datetime.utcnow()
            except Exception:
                event.retry_count += 1
                if event.retry_count >= 5:
                    event.status = "failed"
        await db.commit()

# 3. 服务层使用（与业务事务绑定）
class AuthService:
    async def register(self, data, db: AsyncSession):
        async with db.begin():
            user = User(**data.dict())
            db.add(user)
            # 事件与注册在同一事务 — 要么一起成功，要么一起回滚
            await self._publisher.publish(
                "registered",
                {"user_id": str(user.uid), "phone": user.phone},
                db,
            )

# 工作量：0.5天
```

---

### 必修 2：会员过期无定时处理 — membership_tier 永远不降级

```
当前状态：
  ✅ user.membership.expired 事件已定义
  ❌ 但谁来触发它？

  User 表有 membership_tier 字段
  如果 VIP 到期后没有人把 tier 改回 FREE：
  → 用户永远享受 VIP 权限 → AI 对话配额不限 → 收入损失
```

```python
# ✅ 方案：添加会员过期定时任务

# app/tasks/membership_task.py
class MembershipExpiryTask:
    """定期检查会员过期"""

    async def run_forever(self, shutdown_event: asyncio.Event):
        while not shutdown_event.is_set():
            try:
                await self._check_expiry()
            except Exception as e:
                logger.error(f"Membership check failed: {e}")
            await asyncio.sleep(300)  # 每 5 分钟检查

    async def _check_expiry(self):
        async with self._session_factory() as session:
            async with session.begin():
                now = datetime.utcnow()

                # 查找已过期但仍是 VIP/SVIP 的用户
                stmt = (
                    select(User)
                    .where(User.membership_tier.in_(["vip", "svip"]))
                    .where(User.vip_expires_at.isnot(None))
                    .where(User.vip_expires_at <= now)
                    .with_for_update(skip_locked=True)
                    .limit(100)
                )
                expired_users = (await session.execute(stmt)).scalars().all()

                for user in expired_users:
                    old_tier = user.membership_tier
                    user.membership_tier = "free"
                    user.vip_expires_at = None

                    # 通过 Outbox 发送事件
                    await self._publisher.publish(
                        "membership.expired",
                        {
                            "user_id": str(user.uid),
                            "previous_tier": old_tier,
                            "expired_at": now.isoformat(),
                        },
                        session,
                    )

                    logger.info(
                        "membership_expired",
                        user_id=str(user.uid),
                        old_tier=old_tier,
                    )

                if expired_users:
                    logger.info(f"Processed {len(expired_users)} expired memberships")

# app/main.py lifespan 中启动
membership_task = MembershipExpiryTask(session_factory, publisher)
membership_shutdown = asyncio.Event()
asyncio.create_task(membership_task.run_forever(membership_shutdown))

# 工作量：0.5天
```

---

## 💡 优化建议（不阻塞上线）

### 建议 1：注册接口补充独立限流

```
当前：
  登录限流 ✅ (5次/15分钟)
  注册限流 ❓ 未明确

  登录限流不等于注册限流：
  攻击者可以不断注册垃圾账号而不触发登录限流

建议：
  同一 IP 每小时最多 5 次注册
  同一手机号每天最多 3 次注册尝试

优先级：中 / 工作量：2小时
```

### 建议 2：数据导出改为异步

```
当前：
  GET /api/v1/data-export → 同步返回

  如果用户数据量大（聊天记录/操作日志等）：
  → 请求超时 → 用户重试 → 服务端重复查询

建议：
  POST /api/v1/data-export → 返回 task_id
  GET  /api/v1/data-export/{task_id} → 查询进度/下载

优先级：低 / 工作量：1天
```

### 建议 3：账号注销确认冷静期

```
当前：
  DELETE /api/v1/account → 直接注销？

  如果没有冷静期：
  → 用户误操作立即生效 → 无法恢复 → 合规风险

建议：
  已有 account_deletion_service.py，确认是否实现了 7 天冷静期。
  如果已实现 → 无需修改
  如果未实现 → 补充冷静期 + 取消注销接口

优先级：中 / 工作量：视现有实现
```

### 建议 4：gRPC 补充写操作

```
当前 6 个 gRPC 方法全是读操作。

  其他服务可能需要的写操作：
  - UpdateUserRole    ← 律师认证通过后 legal-service 回调
  - BanUser           ← 管理后台封禁用户
  - UpdateMembership  ← 支付服务确认后升级会员

  当前可通过 Kafka 事件异步处理，gRPC 写操作为可选增强。

优先级：低 / 工作量：1天
```

---

## 📋 验收清单逐项签署

| # | 检查项 | 验收标准 | 结果 |
|---|--------|----------|------|
| 1 | 注册/登录流程 | 正常注册并登录，获取 Token | ✅ 代码完整 |
| 2 | Token 刷新 | 旧 Token 刷新后失效 + 重放检测 | ✅ Rotation 实现 |
| 3 | 密码强度 | 弱密码被拒绝 | ✅ 策略完整 |
| 4 | 错误信息 | 登录失败统一返回防枚举 | ✅ 统一错误 |
| 5 | 审计日志 | 登录/改密等操作有记录 | ✅ AuditLog |
| 6 | JWKS 端点 | 能获取公钥 | ✅ /.well-known/jwks.json |
| 7 | 设备管理 | 能查看和登出设备，上限 5 | ✅ 完整实现 |
| 8 | 会员升级 | 能升级会员等级 | ✅ 接口存在 |
| 9 | gRPC 调用 | 能通过 gRPC 获取用户 | ✅ 6 个方法 |
| 10 | Kafka 事件 | 事件可靠发布 | ⚠️ 需 Outbox |
| 11 | 会员过期 | 到期自动降级 | ⚠️ 需定时任务 |

---

## 📋 验收前必做清单

```
┌──┬──────────────────────────────────────────────┬───────┬──────┐
│  │ 事项                                         │ 工作量 │ 状态 │
├──┼──────────────────────────────────────────────┼───────┼──────┤
│ 1│ 添加 Outbox 表 + 事件发布 Fallback            │ 0.5天 │  ☐  │
│ 2│ 添加会员过期定时任务 + 集成到 lifespan         │ 0.5天 │  ☐  │
├──┼──────────────────────────────────────────────┼───────┼──────┤
│  │ 合计                                         │  1天  │      │
└──┴──────────────────────────────────────────────┴───────┴──────┘
```

---

## 🆚 全平台验收状态总览

```
┌──────────────────┬──────────┬───────────────────────────────────┐
│ 服务             │ 验收状态  │ 说明                              │
├──────────────────┼──────────┼───────────────────────────────────┤
│ Legal Service    │ ✅ 已通过 │ 标杆服务 (95%)                    │
│ User Service     │ ⚠️ 有条件 │ 修复 2 项后通过 (92%)             │
│ AI Service       │ ⚠️ 有条件 │ 补齐 7 处细节后通过 (88%)         │
│ Knowledge Service│ ⚠️ 有条件 │ 随 AI 修复一并完成 (78%)          │
│ Archive Service  │ ⚠️ 有条件 │ 随 AI 修复一并完成 (78%)          │
│ Embedding Service│ ✅ 已通过 │ readiness 检查完善 (85%)          │
├──────────────────┼──────────┼───────────────────────────────────┤
│ 全平台           │ ⚠️ 进行中│ 预计 1 周内全部通过                │
└──────────────────┴──────────┴───────────────────────────────────┘
```

---

## 🎯 最终评价

```
┌──────────────────────────────────────────────────────────────────┐
│                                                                  │
│   用户服务是全平台安全基座，其认证架构已达到行业最佳实践：          │
│                                                                  │
│   🏆 RS256 + JWKS + Refresh Rotation + 重放检测                  │
│      → 超越大多数同类项目                                        │
│                                                                  │
│   🏆 密码策略 + 登录限流 + 设备管理 + 审计日志                    │
│      → 安全纵深防御完备                                          │
│                                                                  │
│   🏆 Pact 契约测试                                               │
│      → 全平台唯一有跨服务契约保障的服务                           │
│                                                                  │
│   两项必修（Outbox + 会员过期）修复后即可正式签发验收。            │
│                                                                  │
│   与 Legal Service 并列为平台两大标杆服务：                       │
│   · Legal Service → 业务治理标杆（状态机/权限/领域隔离）          │
│   · User Service  → 安全架构标杆（RS256/Rotation/契约测试）       │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```