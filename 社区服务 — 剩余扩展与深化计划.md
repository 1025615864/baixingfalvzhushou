# 社区服务 — 剩余扩展与深化计划

基于你已完成的高完成度基础（核心功能 ~95%），剩余工作集中在 **基础设施集成**、**生产加固**、**生态联通** 三个维度。

---

## 一、差距全景图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                     已完成 vs 待扩展 对照图                               │
│                                                                         │
│  ██████████ 已完成                    ░░░░░░░░░░ 待扩展                  │
│                                                                         │
│  业务功能层                                                              │
│  ██████████ CRUD + 点赞 + 收藏 + 举报 + 嵌套评论 + 热门 + 审核 + 管理    │
│  ░░░░ 最佳回答流程 | 话题板块路由 | 法律专区联动 | 搜索               │
│                                                                         │
│  基础设施层                                                              │
│  ██████ 认证中间件 + Kafka Producer + Redis缓存 + UserClient           │
│  ░░░░░░░░ OpenTelemetry | Consul注册 | 统一CORS | 健康检查              │
│           Alembic迁移 | TracingProducer替换 | 生命周期管理              │
│                                                                         │
│  生产加固层                                                              │
│  ██████ 12个单元测试                                                    │
│  ░░░░░░░░░░ 集成测试 | 契约测试 | 限流 | 日志规范 | 错误码体系          │
│             datetime修复 | Dockerfile | CI/CD | Prometheus指标          │
│                                                                         │
│  生态联通层                                                              │
│  ██████ UserClient + AIClient                                           │
│  ░░░░░░░░ Notification联通 | Points联通 | Search联通                   │
│           Recommendation联通 | LegalService联通                        │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 二、扩展任务分类详解

---

### 🔴 A. common 公共组件集成（最高优先级）

这是你的项目架构最大的优势，但社区服务**目前完全没有接入**。其他服务（Backend, User, Legal, Order）都已集成，社区服务是唯一的"孤岛"。

```
当前 main.py 生命周期:
    startup → 创建数据库表 → 结束
    shutdown → 无

目标 main.py 生命周期:
    startup → 创建表
           → init_telemetry()           ← 链路追踪
           → consul.register_service()   ← 服务注册
           → init_kafka_producer()       ← TracingProducer
           → init_redis()               ← Redis连接池
    shutdown → consul.deregister()
            → kafka.close()
            → redis.close()
```

| # | 任务 | 说明 | 工时 |
|---|------|------|------|
| A1 | OpenTelemetry 初始化 | 调用 `init_telemetry("community-service")` | 0.5天 |
| A2 | Consul 服务注册/注销 | 生命周期中注册，关闭时注销 | 0.5天 |
| A3 | 统一 CORS 配置 | 替换硬编码为 `get_cors_config()` | 0.5天 |
| A4 | 健康检查端点 | `/health`, `/health/ready`, `/health/live` | 0.5天 |
| A5 | TracingProducer 替换 | 现有 producer 替换为带链路追踪版本 | 0.5天 |
| A6 | ConfigManager 集成 | 统一配置加载方式 | 0.5天 |

**A4 健康检查设计**：

```python
# 三级健康检查
@router.get("/health")
async def health():
    """基础存活检查"""
    return {"status": "ok", "service": "community-service"}

@router.get("/health/ready")
async def readiness(db: AsyncSession = Depends(get_db)):
    """就绪检查 — 验证所有依赖"""
    checks = {}
    
    # 数据库
    try:
        await db.execute(text("SELECT 1"))
        checks["database"] = "ok"
    except Exception:
        checks["database"] = "fail"
    
    # Redis
    try:
        await redis.ping()
        checks["redis"] = "ok"
    except Exception:
        checks["redis"] = "fail"
    
    # Kafka
    checks["kafka"] = "ok" if event_bus.is_connected() else "fail"
    
    all_ok = all(v == "ok" for v in checks.values())
    return JSONResponse(
        status_code=200 if all_ok else 503,
        content={"status": "ready" if all_ok else "not_ready", "checks": checks}
    )

@router.get("/health/live")
async def liveness():
    """存活检查 — K8s liveness probe"""
    return {"status": "alive", "uptime": get_uptime()}
```

---

### 🟠 B. 生产加固（部署前必须）

#### B1. Alembic 数据库迁移

```
当前: metadata.create_all()  ← 开发可以，生产绝对不行
风险: 无法追踪schema变更、无法回滚、多实例竞争创建表
```

```
需要创建:
services/community-service/
├── alembic/
│   ├── env.py
│   ├── versions/
│   │   ├── 001_initial_tables.py
│   │   ├── 002_add_hot_score_index.py
│   │   └── ...
│   └── alembic.ini
```

#### B2. datetime 修复

```python
# 当前 (Python 3.12+ 已废弃)
created_at = Column(DateTime, default=datetime.utcnow)

# 修复为
from datetime import datetime, timezone
created_at = Column(DateTime(timezone=True), default=lambda: datetime.now(timezone.utc))
```

#### B3. 统一错误码体系

```python
# 当前: 散落的 HTTPException，无统一错误码
raise HTTPException(status_code=404, detail="帖子不存在")

# 目标: 业务错误码体系
class CommunityError:
    POST_NOT_FOUND      = ("C40401", "帖子不存在")
    POST_NO_PERMISSION   = ("C40301", "无权操作此帖子")
    COMMENT_NOT_FOUND    = ("C40402", "评论不存在")
    CONTENT_BLOCKED      = ("C42201", "内容包含违规信息")
    ALREADY_LIKED        = ("C40901", "已经点过赞了")
    RATE_LIMITED         = ("C42901", "操作过于频繁")

# 统一响应格式
{
    "code": "C40401",
    "message": "帖子不存在",
    "detail": null,
    "trace_id": "abc123..."   ← 关联链路追踪
}
```

#### B4. 发帖/评论限流

```python
# middleware/rate_limit.py
class PostRateLimiter:
    """
    防灌水限流规则:
    - 发帖: 每用户每5分钟最多1篇
    - 评论: 每用户每分钟最多5条  
    - 点赞: 每用户每分钟最多20次
    - 举报: 每用户每小时最多10次
    """
    
    async def check_post_rate(self, user_id: int) -> bool:
        key = f"rate:post:{user_id}"
        count = await self.redis.incr(key)
        if count == 1:
            await self.redis.expire(key, 300)  # 5分钟窗口
        return count <= 1
```

#### B5. Dockerfile

```dockerfile
FROM python:3.12-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

EXPOSE 8007

CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8007"]
```

| # | 任务 | 工时 |
|---|------|------|
| B1 | Alembic 迁移脚本 | 1天 |
| B2 | datetime.utcnow 全量替换 | 0.5天 |
| B3 | 统一错误码体系 | 1天 |
| B4 | 发帖/评论限流中间件 | 0.5天 |
| B5 | Dockerfile + .dockerignore | 0.5天 |
| B6 | 结构化日志 (JSON格式 + trace_id) | 0.5天 |
| B7 | Prometheus 指标埋点 | 1天 |

---

### 🟡 C. Kafka 事件补全 + 生态联通

当前 Kafka Producer 已有，但**只发布了部分事件**，且没有与其他微服务形成闭环。

```
┌──────────────────────────────────────────────────────────────────────┐
│                      事件驱动生态联通                                  │
│                                                                      │
│  Community Service                                                   │
│       │                                                              │
│       ├── community.post.created ──────▶ Points Service (+5积分)     │
│       │                          ──────▶ Search Service (索引帖子)   │
│       │                                                              │
│       ├── community.post.liked ────────▶ Notification (通知作者)     │
│       │                          ──────▶ Points Service (+1积分)     │
│       │                                                              │
│       ├── community.comment.created ──▶ Notification Service         │
│       │                                  (通知帖子作者 + 被回复者)    │
│       │                          ──────▶ Points Service (+2积分)     │
│       │                                                              │
│       ├── community.post.favorited ───▶ Recommendation Service       │
│       │                                  (用户偏好学习)               │
│       │                                                              │
│       ├── community.post.reported ────▶ Notification Service         │
│       │                                  (通知管理员)                 │
│       │                                                              │
│       └── community.post.viewed ──────▶ Recommendation Service       │
│                                          (浏览行为分析)               │
│                                                                      │
│  ◀◀◀ 反向消费                                                        │
│                                                                      │
│  user.profile.updated ──────────────▶ Community Service               │
│       (用户改名/换头像)                  (更新冗余的 author_name/avatar)│
│                                                                      │
│  user.lawyer.verified ──────────────▶ Community Service               │
│       (律师认证通过)                    (更新 is_lawyer 标识)          │
└──────────────────────────────────────────────────────────────────────┘
```

**关键缺失：Community Service 没有 Kafka Consumer**

```python
# events/consumer.py  ← 需要新增
class CommunityEventConsumer:
    """
    消费其他服务的事件，保持社区数据一致性
    """
    
    async def handle_user_profile_updated(self, event: dict):
        """用户改名/换头像 → 批量更新帖子和评论中的冗余字段"""
        user_id = event["user_id"]
        new_name = event.get("nickname")
        new_avatar = event.get("avatar")
        
        # 批量更新帖子
        await db.execute(
            update(Post)
            .where(Post.user_id == user_id)
            .values(author_name=new_name, author_avatar=new_avatar)
        )
        
        # 批量更新评论
        await db.execute(
            update(Comment)
            .where(Comment.user_id == user_id)
            .values(author_name=new_name, author_avatar=new_avatar)
        )
    
    async def handle_lawyer_verified(self, event: dict):
        """律师认证通过 → 更新帖子/评论的律师标识"""
        user_id = event["user_id"]
        await db.execute(
            update(Post)
            .where(Post.user_id == user_id)
            .values(is_lawyer=True)
        )
```

| # | 任务 | 工时 |
|---|------|------|
| C1 | 补全所有事件发布点 (收藏/浏览/举报) | 1天 |
| C2 | Kafka Consumer 框架 | 0.5天 |
| C3 | 用户信息变更同步 (冗余字段更新) | 1天 |
| C4 | 事件幂等性保障 (去重机制) | 0.5天 |

---

### 🟡 D. 业务功能补全

#### D1. 话题/板块路由

Topic 模型已存在，但**没有对应的路由和 Service**。

```python
# routers/topic.py  ← 需要新增
@router.get("/topics")
async def list_topics():
    """获取所有话题板块（法律专区）"""
    # 返回: 婚姻家庭、劳动纠纷、交通事故、房产纠纷、合同纠纷...

@router.get("/topics/{topic_id}/posts")
async def get_topic_posts(topic_id: int, page: int = 1):
    """获取指定话题下的帖子列表"""

@router.post("/topics")  # Admin only
async def create_topic():
    """管理员创建话题板块"""
```

#### D2. 最佳回答完整流程

BestAnswer 模型已存在，但**缺少标记/取消的路由**。

```python
# 在 post.py 或 comment.py 中补充
@router.post("/posts/{post_id}/best-answer/{comment_id}")
async def mark_best_answer(
    post_id: int,
    comment_id: int,
    current_user: AuthUser = Depends(get_current_user)
):
    """
    标记最佳回答（仅帖子作者可操作）
    → 触发 Kafka 事件
    → 被标记者获得积分奖励
    → 律师的最佳回答额外加分
    """
```

#### D3. 帖子搜索

```python
# routers/post.py 补充搜索端点
@router.get("/posts/search")
async def search_posts(
    q: str,                    # 关键词
    category: str = None,      # 分类过滤
    is_lawyer: bool = None,    # 仅看律师帖子
    has_best_answer: bool = None,  # 仅看有最佳回答的
    sort_by: str = "relevance"  # relevance / hot / latest
):
    """
    方案A (短期): PostgreSQL 全文搜索 (pg_trgm + ts_vector)
    方案B (长期): 集成 Search Service (Elasticsearch)
    """
```

| # | 任务 | 工时 |
|---|------|------|
| D1 | Topic 话题路由 + TopicService | 1天 |
| D2 | 最佳回答标记/取消流程 | 0.5天 |
| D3 | 帖子搜索 (PostgreSQL全文检索) | 1.5天 |
| D4 | 帖子/评论 排序选项 (最新/最热/精华) | 0.5天 |

---

### ⚪ E. 测试增强

当前 12 个单元测试覆盖了核心逻辑，但缺少以下维度：

```
┌──────────────────────────────────────────────────────────┐
│                    测试金字塔                              │
│                                                          │
│                    ╱╲                                    │
│                   ╱  ╲      E2E 测试                     │
│                  ╱ ❌ ╲     (Playwright/Selenium)        │
│                 ╱──────╲                                 │
│                ╱        ╲                                │
│               ╱   ❌     ╲   集成测试                    │
│              ╱  API 联调  ╲  (TestClient + 真实DB)       │
│             ╱──────────────╲                             │
│            ╱                ╲                            │
│           ╱   ⚠️ 部分覆盖    ╲  契约测试                 │
│          ╱  (Pact Consumer)  ╲ (与User Service的契约)   │
│         ╱────────────────────╲                           │
│        ╱                      ╲                         │
│       ╱     ✅ 12个测试通过     ╲  单元测试              │
│      ╱   (Service层 + 工具函数)  ╲                      │
│     ╱────────────────────────────╲                      │
└──────────────────────────────────────────────────────────┘
```

| # | 任务 | 工时 |
|---|------|------|
| E1 | API 集成测试 (TestClient + SQLite) | 1.5天 |
| E2 | Pact 契约测试 (与 User Service) | 1天 |
| E3 | 审核流程测试 (边界用例) | 0.5天 |
| E4 | 并发测试 (点赞去重、楼层自增竞态) | 0.5天 |

---

### ⚪ F. 性能与缓存优化

```
┌───────────────────────────────────────────────────────────────────┐
│                     缓存策略设计                                    │
│                                                                   │
│  Redis Key                    │  TTL    │  失效策略               │
│  ────────────────────────────│─────────│────────────────────     │
│  post:detail:{id}            │  10min  │  更新/删除时主动失效     │
│  post:list:{category}:{page} │  5min   │  新帖子时失效该分类      │
│  hot:posts:all               │  5min   │  定时任务刷新            │
│  hot:posts:{category}        │  5min   │  定时任务刷新            │
│  hot:topics                  │  10min  │  定时任务刷新            │
│  user:posts:{user_id}:{page} │  5min   │  用户发帖时失效          │
│  post:like_count:{id}        │  30min  │  点赞时 INCR            │
│  rate:post:{user_id}         │  5min   │  自动过期               │
│  rate:comment:{user_id}      │  1min   │  自动过期               │
└───────────────────────────────────────────────────────────────────┘
```

```
数据库查询优化:
│
├── 帖子列表查询
│   当前: 每次查询 SELECT + COUNT
│   优化: 游标分页 (Cursor-based) 替代 OFFSET 分页
│         SELECT * FROM posts WHERE id < :cursor ORDER BY id DESC LIMIT 20
│
├── 热门帖子
│   当前: 实时查询 ORDER BY hot_score
│   优化: Redis ZSET 存储 Top N，定时刷新
│
├── 评论嵌套查询
│   当前: 多次查询组装树
│   优化: 一次查询 + 内存组装 (已做)
│         或 PostgreSQL recursive CTE
│
└── 点赞计数
    当前: UPDATE post SET like_count = like_count + 1
    优化: Redis INCR 实时计数 + 定时回写 DB (削峰)
```

| # | 任务 | 工时 |
|---|------|------|
| F1 | 帖子详情缓存 + 主动失效 | 0.5天 |
| F2 | 游标分页替代 OFFSET 分页 | 1天 |
| F3 | 点赞计数 Redis 缓冲层 | 0.5天 |
| F4 | 数据库索引优化审查 | 0.5天 |

---

## 三、完整实施路线图

```
Phase 1 (第1周) ━━ 基础设施集成 ━━━━━━━━━━━━━━━━ 🔴 不接入就是孤岛
│
├── A1  OpenTelemetry 初始化                      0.5天
├── A2  Consul 服务注册/注销                       0.5天
├── A3  统一 CORS 替换                            0.5天
├── A4  健康检查三端点                             0.5天
├── A5  TracingProducer 替换                      0.5天
├── B2  datetime.utcnow 全量修复                  0.5天
├── B5  Dockerfile                               0.5天
│                                          小计: 3.5天
│
Phase 2 (第2周) ━━ 生产加固 ━━━━━━━━━━━━━━━━━━━━ 🟠 部署前必须
│
├── B1  Alembic 迁移脚本                          1天
├── B3  统一错误码体系                             1天
├── B4  发帖限流中间件                             0.5天
├── B6  结构化日志                                0.5天
├── C1  Kafka 事件发布补全                        1天
│                                          小计: 4天
│
Phase 3 (第3周) ━━ 生态联通 + 功能补全 ━━━━━━━━━━ 🟡 形成闭环
│
├── C2  Kafka Consumer 框架                       0.5天
├── C3  用户信息变更同步                           1天
├── D1  Topic 话题路由                            1天
├── D2  最佳回答流程                              0.5天
├── D4  排序选项扩展                              0.5天
│                                          小计: 3.5天
│
Phase 4 (第4周) ━━ 质量 + 性能 ━━━━━━━━━━━━━━━━━ ⚪ 打磨
│
├── D3  帖子搜索 (PG全文检索)                     1.5天
├── E1  API 集成测试                              1.5天
├── F1  帖子详情缓存                              0.5天
├── B7  Prometheus 指标                           1天
│                                          小计: 4.5天
```

---

## 四、完整任务总表

| # | 任务 | 优先级 | Phase | 工时 |
|---|------|--------|-------|------|
| A1 | OpenTelemetry 初始化 | 🔴 | 1 | 0.5天 |
| A2 | Consul 服务注册/注销 | 🔴 | 1 | 0.5天 |
| A3 | 统一 CORS 替换 | 🔴 | 1 | 0.5天 |
| A4 | 健康检查端点 (/health/*) | 🔴 | 1 | 0.5天 |
| A5 | TracingProducer 替换 | 🔴 | 1 | 0.5天 |
| A6 | ConfigManager 集成 | 🟠 | 1 | 0.5天 |
| B1 | Alembic 迁移脚本 | 🟠 | 2 | 1天 |
| B2 | datetime.utcnow 修复 | 🔴 | 1 | 0.5天 |
| B3 | 统一错误码体系 | 🟠 | 2 | 1天 |
| B4 | 发帖/评论限流 | 🟠 | 2 | 0.5天 |
| B5 | Dockerfile | 🔴 | 1 | 0.5天 |
| B6 | 结构化日志 (JSON + trace_id) | 🟠 | 2 | 0.5天 |
| B7 | Prometheus 指标埋点 | 🟡 | 4 | 1天 |
| C1 | Kafka 事件发布补全 | 🟠 | 2 | 1天 |
| C2 | Kafka Consumer 框架 | 🟡 | 3 | 0.5天 |
| C3 | 用户信息冗余字段同步 | 🟡 | 3 | 1天 |
| C4 | 事件幂等性保障 | 🟡 | 3 | 0.5天 |
| D1 | Topic 话题路由 + Service | 🟡 | 3 | 1天 |
| D2 | 最佳回答标记流程 | 🟡 | 3 | 0.5天 |
| D3 | 帖子搜索 (PG 全文检索) | ⚪ | 4 | 1.5天 |
| D4 | 排序选项 (最新/最热/精华) | 🟡 | 3 | 0.5天 |
| E1 | API 集成测试 | ⚪ | 4 | 1.5天 |
| E2 | Pact 契约测试 | ⚪ | 4 | 1天 |
| E3 | 审核边界测试 | ⚪ | 4 | 0.5天 |
| E4 | 并发竞态测试 | ⚪ | 4 | 0.5天 |
| F1 | 帖子详情缓存 + 失效 | ⚪ | 4 | 0.5天 |
| F2 | 游标分页替代 OFFSET | ⚪ | 4 | 1天 |
| F3 | 点赞计数 Redis 缓冲 | ⚪ | 4 | 0.5天 |
| F4 | 数据库索引审查 | ⚪ | 4 | 0.5天 |

**总计：约 20 个工作日（4 周）**

---

## 五、优先级决策建议

```
如果你只有 1 周 → 做 Phase 1 (A1-A5 + B2 + B5)
    效果: 社区服务接入整体微服务治理体系，可被发现、可被追踪、可部署

如果你有 2 周  → 做 Phase 1 + 2
    效果: 具备生产部署条件

如果你有 3 周  → 做 Phase 1 + 2 + 3
    效果: 与其他服务形成事件驱动闭环，功能完整

如果你有 4 周  → 全部完成
    效果: 生产级社区服务，性能优化、测试完善
```

**核心判断：你的业务功能已经做到 95%，最大的短板是没有接入你自己设计的那套优秀的 common 基础设施。Phase 1 才是真正让社区服务"融入"整体架构的关键一步。**
